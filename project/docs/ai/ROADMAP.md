---
roadmap_version: 2
active_research_workstream_id: stage128-m3i2-final-official-documentary-recovery
predecessor_research_workstream_id: stage128-m3i2-official-source-evidence-capture
qc_scope: stage126-m1-financial-baseline
last_completed_research_action_id: stage128-m3i2-track-a-waiting-termination-and-m3-disposition
next_research_action_id: human-decision-required
next_research_action_authorized: false
next_research_action_scope: no_further_action_is_authorized
conditional_follow_up_action_id: stage128-m3i2-final-official-inquiry-one-follow-up
conditional_follow_up_earliest_date: 2026-08-21
conditional_follow_up_authorized: false
m3i2_inquiry_submission_status: SUBMITTED_ACKNOWLEDGED_WAITING_FOR_SUBSTANTIVE_RESPONSE
m3i2_inquiry_waiting_period_completion_date: 2026-08-20
m3i2_inquiry_waiting_period_status: VOLUNTARILY_TERMINATED_BY_EXPLICIT_HUMAN_DECISION
m3i2_inquiry_waiting_period_termination_date: 2026-08-08
m3i2_track_a_waiting_termination_action_id: stage128-m3i2-track-a-waiting-termination-and-m3-disposition
m3i2_track_a_further_followup_authorized: false
m3i2_track_a_further_wdi_retrieval_authorized: false
m3_macro_data_gate_status: UNRESOLVED_M3_DATA_GATE
m3_macro_data_gate_human_review_required: true
m3_macro_data_gate_executed: true
m3_macro_data_gate_terminal_status: UNRESOLVED_M3_DATA_GATE
m3_macro_data_gate_formal_failure: false
m3_macro_data_gate_unresolved_reason_count: 8
m3_macro_data_gate_unresolved_reasons_resolved: 0
m3_cbi_discontinuation_action_id: stage129-m3-cbi-human-discontinuation-and-reporting
m3_cbi_disposition: M3_CBI_DISCONTINUED_BY_HUMAN_DECISION_UNRESOLVED_DATA_GATE_AND_UNPROVEN_POINT_IN_TIME
m3_cbi_block_admitted: false
m3_cbi_modeling_will_run: false
m3_cbi_incremental_evaluation_will_run: false
m3_cbi_retrieval_continues: false
m3_cbi_reopening_authorized: false
m3_cbi_reopening_requires_new_human_authorization: true
m3_cbi_point_in_time_availability_established: false
m3_cbi_comparison_id: M3_CBI_minus_M2
m3_cbi_comparison_frozen_sap_identifier: M3_minus_M2
m3_cbi_comparison_status: NOT_EXECUTED_M3_CBI_DISCONTINUED
m3_cbi_comparison_p_value: null
m3_cbi_next_action_id: human_decision_required
m3_cbi_next_action_scope: m3_cbi_discontinued_no_further_m3_cbi_action_is_authorized
m3_cbi_next_action_authorized: false
m3_cbi_manuscript_writing_authorized: false
final_model_eligibility_audit_action_id: stage129-final-development-model-eligibility-audit
final_model_eligibility_audit_is_read_only: true
final_block_verdict: FINAL_BLOCK_REQUIRES_HUMAN_DECISION
final_algorithm_verdict: FINAL_ALGORITHM_REQUIRES_HUMAN_DECISION
holm_reporting_verdict: HOLM_FINAL_REPORTING_REQUIRES_SEPARATE_HUMAN_OR_METHODS_DECISION
final_block_determined_candidate: null
final_algorithm_determined_candidate: null
final_model_eligibility_audit_candidate_count: 6
final_model_eligibility_audit_eligible_candidate_count: 6
final_model_eligibility_audit_ordering_stable: false
final_model_eligibility_audit_next_action_id: human_decision_required
final_model_eligibility_audit_next_action_authorized: false
paper_winner_selected: true
final_model_selected: false
full_development_refit_performed: false
stage130_started: false
final_selection_action_id: stage129-final-model-human-selection-governance
final_development_block_selected: true
final_development_block: M1
final_algorithm_selected: true
final_algorithm: regularized_logistic_regression
final_configuration: logistic__C_0.1
final_configuration_was_prelocked_before_this_decision: true
final_configuration_selected_by_this_decision: false
selection_is_human_governance_decision: true
selection_basis: HUMAN_DECISION_BASED_ON_PRELOCKED_DEVELOPMENT_EVIDENCE
inferential_superiority_claimed: false
trained_final_model_artifact_created: false
final_holm_reporting_status: HOLM_NOT_EXECUTED_FAMILY_PRESERVED_NO_INFERENCE
final_selection_next_action_id: human_authorization_required_for_full_development_refit_and_final_test
final_selection_next_action_authorized: false
refit_contract_action_id: stage129-full-development-refit-contract-lock
refit_contract_id: stage129_full_development_refit_contract
refit_contract_status: PROSPECTIVELY_LOCKED_NOT_EXECUTED
refit_fit_target_years: 1393-1399
refit_final_test_target_years: 1400-1402
refit_feature_count: 9
refit_threshold_rule: development_OOF_F2_maximizing_threshold
refit_fail_closed_control_count: 12
refit_terms_extracted_not_invented: true
refit_executed: false
refit_execution_authorized: false
refit_execution_requires_new_human_authorization: true
refit_next_action_id: human_authorization_required_for_full_development_refit_execution
refit_next_action_authorized: false
refit_execution_action_id: stage129-full-development-refit-execution
refit_execution_executed: true
refit_execution_model_fits: 1
refit_execution_fit_set_rows: 666
refit_execution_fit_set_positive: 68
refit_execution_fit_set_negative: 598
refit_execution_fit_target_years: 1393-1399
refit_execution_controls_evaluated: 12
refit_execution_controls_all_passed: true
refit_execution_new_scientific_result_produced: false
refit_execution_predictions_generated: 0
refit_execution_final_test_rows_read: 0
refit_execution_next_action_id: human_authorization_required_for_final_test_access
refit_execution_next_action_authorized: false
m3i2_contract_status: PROSPECTIVELY_LOCKED_NO_DATA
m3i2_data_retrieval_started: false
m3i2_data_gate_executed: false
m3i3_financing_metadata_lock: UNRESOLVED_METADATA_LOCK
m3i2_official_source_evidence_status: UNRESOLVED_OFFICIAL_SOURCE_EVIDENCE
m3i2_final_documentary_recovery_status: HUMAN_SUBMISSION_REQUIRED
m3_lag_wdi_authoritative_contract_status: AUTHORITATIVE_CONTRACT_LOCKED_PRE_RETRIEVAL
m3_lag_wdi_exploratory_track: PARALLEL_ACTIVE
m3_lag_wdi_last_completed_action_id: stage128-m3-lag-wdi-exploratory-incremental-evaluation
m3_lag_wdi_next_action_id: human_decision_required
m3_lag_wdi_next_action_scope: no_further_action_is_authorized
m3_lag_wdi_next_action_authorized: false
m3_lag_wdi_next_action_executes_data_gate: false
m3_lag_wdi_next_action_executes_data_gate_semantics: describes_the_named_next_action_not_a_permission
m3_lag_wdi_final_research_disposition: SUPPLEMENTARY_EXPLORATORY_ONLY
m4_block_disposition: M4_DISCONTINUED_BY_HUMAN_DECISION_DATA_INADEQUACY
m4_discontinuation_action_id: stage129-m4-human-discontinuation-data-inadequacy
m4_discontinuation_authorized_by_human: true
m4_data_gate_executed: false
m4_formal_gate_verdict: null
m4_retrieval_continues: false
m4_manual_completion_continues: false
m4_modeling_will_run: false
m4_incremental_evaluation_will_run: false
m4_reopening_authorized: false
m4_reopening_requires_new_human_authorization: true
m4_next_action_id: human_decision_required
m4_next_action_scope: m4_discontinued_no_further_m4_action_is_authorized
m4_next_action_authorized: false
m4_manuscript_reporting_decision_action_id: stage129-m4-manuscript-reporting-decision
m4_manuscript_reporting_decision: REPORT_AS_PRESPECIFIED_NOT_EXECUTED_DATA_INADEQUACY_NO_INFERENCE
m4_manuscript_reporting_decision_resolved: true
m4_manuscript_reporting_decision_previous_value: UNRESOLVED_REPORTING_DECISION
m4_comparison_id: M4_minus_M3_CBI
m4_comparison_status: NOT_EXECUTED_M4_DISCONTINUED
m4_comparison_p_value: null
m4_manuscript_writing_authorized: false
m3_lag_wdi_promoted_to_confirmatory_model: false
m3_lag_wdi_point_in_time_wdi_availability_status: UNVERIFIED_WITH_CURRENTLY_AVAILABLE_EVIDENCE
m3_lag_wdi_data_gate_executed: true
m3_lag_wdi_retrieval_action_id: stage128-m3-lag-wdi-exploratory-data-retrieval
m3_lag_wdi_retrieval_status: RETRIEVAL_COMPLETED_RAW_SOURCE_RETAINED
m3_lag_wdi_retrieval_authorization_consumed: true
m3_lag_wdi_further_retrieval_authorized: false
m3_lag_wdi_post_retrieval_audit_action_id: stage128-m3-lag-wdi-exploratory-post-retrieval-audit
m3_lag_wdi_post_retrieval_audit_was_authorized: true
m3_lag_wdi_post_retrieval_audit_authorized: false
m3_lag_wdi_post_retrieval_audit_executed: true
m3_lag_wdi_post_retrieval_audit_result: PASS_WITH_MATERIAL_FINDINGS
m3_lag_wdi_post_retrieval_audit_authorization_consumed: true
m3_lag_wdi_data_gate_action_id: stage128-m3-lag-wdi-exploratory-data-gate
m3_lag_wdi_data_gate_was_authorized: true
m3_lag_wdi_data_gate_authorized: false
m3_lag_wdi_data_gate_authorization_consumed: true
m3_lag_wdi_data_gate_authorization_reusable: false
m3_lag_wdi_data_gate_result: PASS_M3_LAG_WDI_DATA_GATE
m3_lag_wdi_block_admitted: true
m3_lag_wdi_block_admission_is_data_admission_only: true
m3_lag_wdi_gate_block_common_sample_coverage: 1.0
m3_lag_wdi_gate_rows_excluded: 0
m3_lag_wdi_gate_pass_is_information_content_claim: false
m3_lag_wdi_calendar_mapping_locked: true
m3_lag_wdi_calendar_mapping_rule: jalali_fiscal_year_t_plus_621
m3_lag_wdi_calendar_mapping_lock_action_id: stage128-m3-lag-wdi-exploratory-calendar-mapping-lock
m3_lag_wdi_calendar_mapping_lock_was_authorized: true
m3_lag_wdi_calendar_mapping_lock_authorized: false
m3_lag_wdi_calendar_mapping_lock_authorization_consumed: true
m3_lag_wdi_calendar_mapping_lock_authorization_reusable: false
m3_lag_wdi_calendar_mapping_rejected_offset: 622
m3_lag_wdi_calendar_mapping_rejected_offset_violations: 22
m3_lag_wdi_calendar_mapping_lock_required_before_modeling: false
m3_lag_wdi_retrieval_authorization_implies_gate_authorization: false
m3_lag_wdi_combined_retrieval_and_gate_action_permitted: false
m3_lag_wdi_gate_pass_is_data_admission_only: true
m3_lag_wdi_gate_pass_authorizes_modeling: false
m3_lag_wdi_modeling_action_id: stage128-m3-lag-wdi-exploratory-incremental-evaluation
m3_lag_wdi_modeling_was_authorized: true
m3_lag_wdi_modeling_authorized: false
m3_lag_wdi_modeling_authorization_consumed: true
m3_lag_wdi_modeling_authorization_reusable: false
m3_lag_wdi_modeling_executed: true
m3_lag_wdi_evaluation_hypothesis_id: E1
m3_lag_wdi_evaluation_family_id: M3_LAG_WDI_EXPLORATORY_SUPPLEMENTARY
m3_lag_wdi_evaluation_results_label: supplementary_exploratory_robustness_only
m3_lag_wdi_evaluation_e1_conclusion: E1_NULL_NO_DETECTABLE_INCREMENTAL_CONTRIBUTION
m3_lag_wdi_evaluation_primary_metric: pr_auc
m3_lag_wdi_evaluation_secondary_metric_signal_present: true
m3_lag_wdi_evaluation_secondary_metric_signal_is_confirmatory: false
m3_lag_wdi_evaluation_rows: 539
m3_lag_wdi_evaluation_m2_feature_count: 12
m3_lag_wdi_evaluation_block_feature_count: 14
m3_lag_wdi_evaluation_primary_predictive_fits: 44
m3_lag_wdi_confirmatory_holm_family_changed: false
m3_lag_wdi_confirmatory_superiority_claim_made: false
m3_lag_wdi_paper_winner_selected: false
m3_lag_wdi_final_test_rows_read: 0
m3_lag_wdi_data_retrieval_started: true
m3_lag_wdi_data_gate_executed: true
m3_lag_wdi_modeling_started: true
m3i2_documentary_recovery_pr_number: 76
m3i2_documentary_recovery_pr_merge_commit: 89d8e6ff2d12ec82903cd28aa7ab839eb946b658
m3i2_human_submission_pr_number: 77
m3i2_human_submission_pr_merge_commit: 93de6bae9344ce893b0261f818abce8a991cf842
m3_lag_contract_lock_pr_number: 78
m3_lag_contract_lock_pr_merged: true
m3_lag_contract_lock_pr_merge_commit: 175e7949e009eeecdd66aedab31ec4b48e9d3c7d
m3_lag_data_retrieval_pr_number: 79
m3i2_live_draft_pr_number: 79
active_maintenance_task_id: repository-driven-ai-handoff
---

# ROADMAP

The front matter above is the **machine-readable** roadmap pointer. Research work and maintenance work remain separate: maintenance never advances a research action.

**Workstream identifier note (updated at the M3I-2 final official documentary recovery).** `active_research_workstream_id` names the workstream that is live **now**: `stage128-m3i2-final-official-documentary-recovery` (item 25c). Item 25b (`stage128-m3i2-official-source-evidence-capture`, PR #75) is its **merged predecessor context**. The live action is an INITIATION: a bounded official documentary search (14 GET requests of a maximum of 20, World Bank Group hosts only, no archive ZIP and no repeat of a previously captured URL) plus the preparation of exactly ONE official World Bank Data Help Desk inquiry. It executed no Data Gate, computed no coverage, materialized no feature, fit no model and touched no Final Test row. The bounded search returned `NO_NEW_DOCUMENTARY_EVIDENCE_IN_BOUNDED_SEARCH` — the official WDI release notes begin only in December 2024, the official Data Updates page announces database updates rather than archive-edition publications, and no official source states the Iranian LCU denomination, valuation convention or unit continuity for `PA.NUS.FCRF` — so both blockers remain open and every edition keeps `available_at = null`, `release_date_verified = false`. The inquiry could not be submitted by automation (the Help Desk requires a signed-in account), so the initiation ended at `HUMAN_SUBMISSION_REQUIRED`, no ticket was opened and none was invented.

**Human submission recorded (item 25d, COMPLETE).** A **human supervisor** has since submitted that prepared inquiry **exactly once**, through the official World Bank Data Help Desk *Contact support* channel under the category *Data Compilation Methodology*, with both public attachments visibly selected. The live submission status is therefore `SUBMITTED_ACKNOWLEDGED_WAITING_FOR_SUBSTANTIVE_RESPONSE`. An **acknowledgement** was received (the confirmation page and a confirmation e-mail); an acknowledgement is a **receipt, not an answer**, and it resolves neither blocker. **No ticket id was displayed and none was fabricated**; the confirmation UI displayed `2026-08-06 14:03` **without a timezone**, so the UTC instant stays `null` with status `UNRESOLVED_CONFIRMATION_UI_DID_NOT_DISPLAY_TIMEZONE` and was **not** inferred from the user's location, the system clock or a screenshot filename. The canonical body was seen repeated in the e-mail but not verified byte-for-byte (`CANONICAL_BODY_VISUALLY_CONFIRMED_NOT_RAW_BYTE_VERIFIED`), and the confirmation never enumerated the attachments server-side. Every raw confirmation copy is stored **outside** the repository and only its SHA-256 and byte size are committed — no screenshot, e-mail, name, address or account identifier is in Git. The locked stopping rule (10 business days, Mon-Fri, submission day excluded) runs from the official calendar date **2026-08-06** and completes on **2026-08-20**; the earliest a follow-up could even be *considered* is **2026-08-21**, and that is eligibility only. The authoritative pointers are `last_completed_research_action_id: stage128-m3i2-final-official-inquiry-human-submission` and `next_research_action_id: stage128-m3i2-final-official-inquiry-response-ingestion` with `next_research_action_authorized: false`, plus the separate conditional pointer `conditional_follow_up_action_id: stage128-m3i2-final-official-inquiry-one-follow-up` (`conditional_follow_up_earliest_date: 2026-08-21`, `conditional_follow_up_authorized: false`). **Nothing scientific moved:** M3I-2 stays `UNRESOLVED_OFFICIAL_SOURCE_EVIDENCE` and unadmitted, all 37 cutoffs and all 539 development pairs stay unresolved, CPI stays 16 PASS and FX stays 16 UNRESOLVED, the Data Gate stays `NOT_EXECUTED`, M3-LAG-WDI was `NOT_LOCKED` **as of this item 25d** (superseded at item 25h below, where it becomes `AUTHORITATIVE_CONTRACT_LOCKED_PRE_RETRIEVAL`), the Final Test stays locked and M4 stays unauthorized.

**TWO PARALLEL TRACKS (item 25h).** The repository now runs two tracks at once, and they must not be confused with one another.

**TRACK A — World Bank official inquiry: still waiting.** Status `SUBMITTED_ACKNOWLEDGED_WAITING_FOR_SUBSTANTIVE_RESPONSE`; the waiting period stays **ACTIVE through 2026-08-20**; the earliest possible follow-up stays **2026-08-21**; follow-up is **not authorized**; response ingestion and adjudication are **not authorized**. Track A owns the authoritative research pointers in the front matter and is untouched by Track B.

**TRACK B — `stage128-m3-lag-wdi-exploratory-contract-lock` (item 25h, COMPLETE): the M3-LAG-WDI exploratory contract is LOCKED, pre-retrieval.** Recorded under its own NEW explicit one-action human authorization (158 UTF-8 bytes, SHA-256 `0c1e1049…995b`; the exact utterance lives only in `project/stage128/m3_lag_wdi_exploratory_contract_lock/stage128_m3_lag_wdi_exploratory_human_authorization_record.json`), which was CONSUMED by this contract lock and is **not** reusable for retrieval, for the Data Gate or for modeling. The authoritative status is `m3_lag_wdi_authoritative_contract_status: AUTHORITATIVE_CONTRACT_LOCKED_PRE_RETRIEVAL`. The contract freezes, before any value is seen: the **exploratory-only** role (`supplementary_exploratory_robustness_block` — not confirmatory M3, not a replacement or repair of M3-CBI, not a continuation of M3I-2, not able by itself to select the paper winner); **exactly two** additional macro features — `intl_cpi_inflation_lag1_wdi` (`FP.CPI.TOTL.ZG`, `IRN`, observation year `t-1`, identity) and `intl_fx_change_official_lag1_wdi` (`PA.NUS.FCRF`, `IRN`, `FX_LAG1_t = 100 * ln(E_(t-1) / E_(t-2))`, both observations present, numeric, strictly positive and consecutive, otherwise NULL), with no third macro feature, no financing-rate feature, no indicator search, no imputation and no alternative FX series after a failure; the retained-M2 **539-row** development parent sample (never the 666-row M1 sample), **12** M2 features versus **14** M3-LAG-WDI features on complete cases for both new features, with **both** models refitted on the identical resulting common sample; the **inherited** Gate thresholds (candidate coverage `>= 0.80`, block coverage `>= 0.70`, `>= 5` positives in each locked validation window, development-only); and the three already-frozen model families with no retuning and no search. The one-year lag is a **conservative temporal-separation design only**: the future retrieval uses the then-current/latest **revised** WDI, so historical-vintage and point-in-time availability are explicitly **not** claimed — which is exactly why this block is exploratory/supplementary. Its comparison lives in a **separate** exploratory family and is **never** inserted into the confirmatory Holm family (`M2_minus_M1`, `M3_CBI_minus_M2`, `M4_minus_M3_CBI`). **Nothing was executed:** 0 World Bank API requests, 0 WDI downloads, 0 value inspections, 0 company-row macro joins, 0 feature materializations, 0 coverage calculations (every coverage value is `null`, **not** zero), 0 Data Gate executions, 0 model fits, 0 predictions, 0 predictive metrics, 0 bootstrap executions, 0 Holm calculations, 0 Final Test rows read. The Track B pointer pair is `m3_lag_wdi_last_completed_action_id: stage128-m3-lag-wdi-exploratory-contract-lock` / `m3_lag_wdi_next_action_id: stage128-m3-lag-wdi-exploratory-data-retrieval` with `m3_lag_wdi_next_action_authorized: false` — retrieval, the Data Gate and any predictive evaluation each need their own separate explicit human authorization. M2 remains `RETAIN_M2_AS_INTERMEDIATE_CONFIRMATORY_BLOCK`, M3-CBI remains `UNRESOLVED_M3_DATA_GATE`, M3I-2 remains `UNRESOLVED_OFFICIAL_SOURCE_EVIDENCE` and unadmitted, the Final Test stays hard locked and M4 stays unauthorized. See `project/stage128/m3_lag_wdi_exploratory_contract_lock/README_STAGE128_M3_LAG_WDI_EXPLORATORY_CONTRACT_LOCK.md`.

Activating Track B does **not** mean the World Bank inquiry failed, terminated, was abandoned or became unnecessary. Track A stays open on its own timetable.

**PR HISTORY IS PINNED, NOT RE-DERIVED (item 25h; live topology re-anchored at item 25i).** Four PRs, four different actions, four different roles. Re-anchoring the LIVE topology onto a newer Draft never rewrites what an older PR *was*, and "the recovery PR" is a **name for a specific historical action**, never a moving label for "whatever merged most recently":

* **PR #76** — `stage128-m3i2-final-official-documentary-recovery-initiation`, the final official documentary recovery **INITIATION**. MERGED by merge commit `89d8e6ff2d12ec82903cd28aa7ab839eb946b658`; semantics `merged_predecessor_superseded_by_pr77`. This is the PR that `m3i2_documentary_recovery_pr_number` / `stage128_m3i2_recovery_pr_number` names, permanently.
* **PR #77** — `stage128-m3i2-final-official-inquiry-human-submission`, the later **HUMAN World Bank inquiry submission RECORDING**, a separate action from the initiation above. MERGED by merge commit `93de6bae9344ce893b0261f818abce8a991cf842`; semantics `merged_predecessor_superseded_by_pr78`. It is the immediate merged predecessor of the live PR, and it is **not** the documentary recovery.
* **PR #78** — `m3_lag_wdi_exploratory_contract_lock_pr`, the M3-LAG-WDI exploratory contract lock. **MERGED** by merge commit `175e7949e009eeecdd66aedab31ec4b48e9d3c7d`; semantics `merged_predecessor_superseded_by_pr79`. It is the immediate merged predecessor of the live PR and is **no longer** the live Draft.
* **PR #79** — `m3_lag_wdi_exploratory_data_retrieval_pr`, the M3-LAG-WDI exploratory data retrieval (item 25i). The **current LIVE Draft**: base `main` @ `175e7949e009eeecdd66aedab31ec4b48e9d3c7d`, draft = true, merged = false, ready-for-review **not** authorized, merge **not** authorized.

A merged predecessor may never be re-published as the live Draft. The generator, the contract-lock suite and the independent current-state validator all pin **both** halves of each merge — the PR number *and* its merge commit — and fail closed if a PR recorded as merged reappears as the open Draft.

The three roles may never be collapsed into one another or shifted forward, and both the Handoff generator and the independent current-state validator fail closed if any of them moves.

**HISTORICAL — SUPERSEDED BY NEW EXPLICIT HUMAN AUTHORIZATION (this paragraph is retained as history, not as live state).** Until item 25h the repository recorded: *"M3-LAG-WDI-EXPLORATORY is NOT locked. A local, uncommitted draft of the M3-LAG-WDI exploratory contract was partially materialized before the human supervisor superseded that path. It produced no data retrieval, Gate, modeling or Final Test access and never became an authoritative repository contract. It is quarantined outside the repository and left untouched, its authorization is consumed and not reusable, and the authoritative status stays `m3_lag_wdi_authoritative_contract_status: NOT_LOCKED`. That path may only be considered in a separate action, under its own authorization, after the official-inquiry stopping rule terminates in `UNRESOLVED_AFTER_FINAL_OFFICIAL_INQUIRY`."* That was accurate **at that time**. The **only** part now superseded is the wait-for-terminal-inquiry restriction, superseded by the new explicit human authorization recorded in item 25h. Everything else about the old draft still holds: it remains quarantined outside the repository, untouched, non-authoritative, and its old authorization remains consumed and non-reusable — it was **not** promoted and **not** committed.

**Workstream identifier note (HISTORICAL — written at the M3I-2 official-source evidence capture; this paragraph is retained only as historical state).** At that time `active_research_workstream_id` named `stage128-m3i2-official-source-evidence-capture` (item 25b), the supplementary international-macro **evidence-acquisition** workstream, which **was live at that time** and is now the **merged predecessor** (PR #75) of the live workstream. Item 25a (`stage128-m3i2-prospective-contract-lock`) was its immediate predecessor context. The workstream that is live **now** is `stage128-m3i2-final-official-documentary-recovery` (item 25c), carried by the live Draft **PR #76** — see the paragraph above. Evidence capture retrieves and hashes official source material; it is **not** a Data Gate and admits nothing. A contract lock is metadata only — `m3i2_data_retrieval_started: false`, `m3i2_data_gate_executed: false`, `m3i2_modeling_started: false` — and it does **not** replace or repair the CBI block. `predecessor_research_workstream_id: stage128-m3-macro-data-gate` was predecessor context at that time; the pointer pair recorded **at that time** was `last_completed_research_action_id: stage128-m3i2-official-source-evidence-capture` / `next_research_action_id: stage128-m3i2-official-source-evidence-review` with `next_research_action_authorized: false` — now historical pointer state, superseded by the live pair in the paragraph above. The paragraph below records the state as of the preceding M3 macro data-Gate PR and remains correct history for that action.

**Historical note (as of the M3 macro data-Gate PR).** The M3 macro DATA workstream is started (`m3_data_workstream_started: true`) because the M3 macro data Gate has been EXECUTED once as a data-admission Gate under its own consumed one-action authorization, returning `UNRESOLVED_M3_DATA_GATE` and awaiting human review. **Gate execution is not modeling:** `m3_modeling_started: false`, `m3_incremental_evaluation_authorized: false`, `m3_block_admitted_for_incremental_evaluation: false`. The live pointer pair **at that time** was `last_completed_research_action_id: stage128-m2-retained-block-human-decision` (correctly **unchanged**, because the Gate is unresolved and awaiting human review) and `next_research_action_id: stage128-m3-macro-data-gate` — a pointer, and a pointer is **never** an authorization. Both are now historical: the live pointers are in the front matter above and name item 25a. `predecessor_research_workstream_id: stage128-m2-d2-boundary-month-equity-return` is **historical/predecessor context**, no longer the current workstream; it was correct while the M2 D2 boundary-month workstream was live (items 22-24b). The still earlier value `stage126-m1-financial-baseline` remains correct **history** for the completed M1 financial-baseline workstream (items 1-20). The pointer pairs `stage127-m2-incremental-evaluation` / `stage128-m2-retained-block-human-decision`, `stage128-m2-boundary-month-return-design-freeze` / `stage128-m2-d2-gate-rerun` and `stage128-m2-d2-gate-rerun` / `stage127-m2-incremental-evaluation` are now **historical** pointer state. `qc_scope` is a separate role and still points at the last completed scientific micro-part's QC scope (`stage126-m1-financial-baseline`).

**Pointer state note (as of the Stage128 M3 macro data-Gate PR):** PR #72 is **merged** on `main` (baseline `35aaf4b70e9341704ee38be6f8cf2e2519c70bb2`), so the retained-block pointer state in the front matter above is **live on `main`**, not conditional. `stage128-m2-retained-block-human-decision` (item 24b) was RECORDED once under its own explicit, one-action human authorization (SHA-256 `91edbded…6e2b4`, 240 UTF-8 bytes, 2026-08-01; the exact utterance lives only in `project/stage128/m2_retained_block_human_decision/stage128_m2_retained_block_human_authorization_record.json`), which was CONSUMED by that recording. Its predecessor `stage127-m2-incremental-evaluation` (item 24) was itself executed once under its own consumed authorization (`بریم مرحله بعد`, SHA-256 `a9999c0c…c3cdc6`). `stage128-m3-macro-data-gate` (item 25) has now been EXECUTED once as a **data-admission Gate only**, under its own explicit one-action authorization (SHA-256 `d4acc969…d23068`, 28 UTF-8 bytes, 2026-08-01; the exact utterance lives only in `project/stage128/m3_macro_data_gate/stage128_m3_macro_data_gate_human_authorization_record.json`), which was CONSUMED by that execution. Its observed result is `UNRESOLVED_M3_DATA_GATE`, so the research pointer **did not advance**: `last_completed_research_action_id` remains `stage128-m2-retained-block-human-decision` and `stage128-m3-incremental-evaluation` was **not** created as a pointer. The Gate fit no model, computed no predictive metric, ran no M3-versus-M2 comparison and left the final test locked.

From 2026-07-26 onward, Stage126+ follows [`STAGE126_Q1Q2_LEAN_GOVERNANCE.md`](STAGE126_Q1Q2_LEAN_GOVERNANCE.md). Scientific contracts/results/final-test controls remain hard-locked; tests, QC formatting, metadata, Handoff and repository bookkeeping are operational surfaces tracked by Git rather than recursively frozen scientific artifacts.

## Research actions (ordered)

1. `stage124-batch02-part03-1a-5` — reviewed-evidence engine
2. `stage124-batch02-part03-1a-5-1` — partial-date manual review + precision compatibility
3. `stage124-batch02-part03-1a-5-2` — cross-record evidence aggregation, deterministic canonical
4. `stage124-batch02-part03-1a-5-3` — unified decision engine, full research↔provenance QC
5. `stage124-batch02-part03-1b-0` — Research-Intake Readiness, baseline unlock, auditable intake scaffold ✅
6. `stage124-batch02-part03-1b-1` — manual source discovery, snapshot capture, and reviewed-evidence intake for the 10 Part 3 tickers — **superseded / cancelled by official TSE API** (not completed)
7. `stage124-official-api-finalize` — Finalized verified master for 130 tickers using official TSETMC first-observed-trade dates ✅
8. `stage124-gate-b-readiness` — Gate B readiness dry-run: three eligibility rules compared ✅
9. `stage124-gate-b-rule-approval` — Rule A primary + Rule B listing-timing robustness approved; Rule C rejected ✅
10. `stage124-gate-b-execution` — approved Gate B rules executed; four sample designs produced ✅
11. `stage125-research-design-readiness` — Stage125 Research Design & Data Readiness; no modeling ✅
12. `stage125-part3a-decision-lock` — accessibility/pilot decision lock ✅
13. `stage125-part3b-evidence-capture` — historical origin probes; broad expansion superseded
14. `stage125-part3b-conservative-lag-decision-lock` — conservative-lag decision history ✅
15. `stage125-part3c-leakage-safe-dataset-finalization` — active four-Jalali-month point-in-time finalization ✅
16. `stage125-part4-statistical-analysis-plan` — M1–M4 SAP, temporal CV, metrics, tuning budget, uncertainty plan ✅
17. `stage125-part5-readiness-closure` — Stage125 Gate 125.0 / Stage126 M1 readiness closure ✅
18. `stage126-m1-financial-baseline` — **COMPLETE.** Primary M1 development tuning is complete. All six registered robustness categories (Parts 1–6, including Part 6 `smote_training_fold_only_robustness`) are complete on `main`. No full-development refit; final test locked; M2/M3/M4 not started.
19. `stage126-m1-robustness-closure` — **COMPLETE.** Verified all six pre-registered M1 robustness analyses (Parts 1-6), synthesized sensitivity evidence from already-committed artifacts (zero model fits/predictions/resampling/final-test access), and closed the robustness set. No retuning, no final-test access, no automatic winner selection. No retained design selected or frozen.
20. `stage126-m1-retained-design-freeze` — **COMPLETE.** Froze the exact retained M1 design using development evidence only: sample/target/features/preprocessing/configuration/metric definitions/uncertainty plan (PR #65). No model fit, no retuning, no final-test access, no winner selection, no M2 start. Final test remains locked.
21. `stage127-m2-market-data-gate` — **EXECUTED AND RESOLVED FROM IMPORTED AUTHORITATIVE EVIDENCE; terminal observed result `FAIL_M2_DATA_GATE`.** The point-in-time accessibility/coverage/join/quality/event-count Gate for the frozen three-variable M2 market block was executed development-only (666 pairs, target years 1393-1399). No modeling was performed and no final-test predictor or target value was parsed, inspected or used. Human authorization for this Gate already existed; the Gate was first executed with no data available and returned UNRESOLVED, authoritative TSETMC evidence was then obtained externally under the same Gate scope, and the Gate was re-executed offline from that immutable bundle (SHA256 `d8456b50…c6c6ec`; 110 mappings, 111 ranges, 105 SUCCESS / 6 PARTIAL, 163,230 normalized daily rows, 222 SHA256-verified raw files, all independently revalidated in this repository). The shared-window end rule is applied LITERALLY: T* is the last eligible trading day with verified available_at strictly before the pair cutoff, chosen independently of whether adjusted_close is present, so the frozen `Require P_t0 and P_tN present` endpoint condition can genuinely fail. G01-G08 all PASS (accessibility 5, derived from the frozen R-A mapping) and both locked validation windows clear the >=5-positive rule (11 / 5), but the OBSERVED development coverage of `equity_return_window` is 0.4039 (269/666, threshold 0.80) and the three-variable common-sample coverage is 0.4039 (269/666, threshold 0.70). `realized_volatility` and `amihud_illiquidity` both reach 0.8649 (576/666). This is an observed threshold failure, deliberately **not** softened into UNRESOLVED. No threshold was reduced, no value imputed, no unadjusted close substituted, no T* chosen to improve coverage, and the frozen three-variable block was not redefined or reduced. M2 market evidence IS collected and independently validated; that is recorded separately from block admission, which did not occur. **Human review historically completed** by the separately authorized `stage128-m2-boundary-month-return-design-freeze` action (item 22); the historical D0 Gate result remains `FAIL_M2_DATA_GATE` and is never rewritten. This item is a HISTORICAL, completed and resolved Gate — it is no longer the current scientific action and is no longer awaiting a human decision.
22. `stage128-m2-boundary-month-return-design-freeze` — **COMPLETE (design-freeze only; no Gate execution).** Freezes `BOUNDARY_MONTH_ASOF_TRAILING_EQUITY_RETURN` (Gregorian calendar convention) as the new PRIMARY M2 equity-return construct, replacing only the D0 exact-endpoint `equity_return_window` measurement component; `W`, `t0`, `T*`, trading-day sequence, daily-return adjacency, the 126-return floor, `realized_volatility` and `amihud_illiquidity` are unchanged. Chosen prospectively, after predictor-only feasibility assessment and before any outcome/model evaluation, for coherence with the frozen Gregorian market-time axis — not because it clears a coverage threshold. The historical D0 Gate result (`FAIL_M2_DATA_GATE`, 269/666) is preserved byte-for-byte and unchanged. No canonical Gate is re-executed, no model is fit, no prediction is generated, and no final-test predictor or target value was parsed, inspected or used. See [`STAGE128_M2_D2_DESIGN_FREEZE.md`](STAGE128_M2_D2_DESIGN_FREEZE.md) and `project/stage128/`. **Naming note:** this action's `stage128-` prefix was specified verbatim in its human authorization and predates/is independent of the pre-existing `stage128-m3-macro-data-gate` / `stage128-m3-incremental-evaluation` items below (items 25-26); both use the `stage128-` label for unrelated M2/M3 actions by coincidence of numbering, not because M3 work has started. Action IDs, not list position, are authoritative.
23. `stage128-m2-d2-gate-rerun` — **COMPLETE; EXECUTED ONCE UNDER ITS OWN EXPLICIT HUMAN AUTHORIZATION; terminal observed result `PASS_FOR_M2_INCREMENTAL_EVALUATION` (DATA ADMISSION ONLY).** The canonical M2 admission Gate was re-executed development-only (666 pairs, target years 1393-1399), offline and deterministically from the SAME immutable TSETMC bundle as the historical Gate (SHA256 `d8456b50…c6c6ec`; 163,230 normalized daily rows, 110 mappings, 111 ranges, 222 SHA256-verified raw files), with EXACTLY ONE difference: the equity-return slot of the frozen three-variable M2 block is measured under the already-frozen Gregorian D2 construct from item 22. `W`, `t0`, `T*`, the trading-day sequence, daily-return adjacency, realized volatility, Amihud and both 126 floors are the unchanged frozen Stage127 primitives, called directly; the Gate conditions A-F and their thresholds are the unchanged frozen Stage127 decision machinery. Observed: `equity_return_window` (D2) 539/666 = 0.8093 (threshold 0.80) ✓, `realized_volatility` 576/666 = 0.8649 ✓, `amihud_illiquidity` 576/666 = 0.8649 ✓, three-variable common sample 539/666 = 0.8093 (threshold 0.70) ✓, and both locked validation windows clear the ≥5-positive rule (fold1_validation 18, fold2_validation 10). A-F all PASS. No threshold was changed, no criterion added or removed, no boundary tolerance searched, no design reopened, and the pre-lock 539/666 predictor-only figure was NOT an input — it was reproduced canonically and compared only afterwards (difference 0). **This is DATA ADMISSION only: it does not say M2 improves prediction.** No model fit, no prediction, no predictive metric, and no final-test predictor or target value was parsed, inspected or used. The one-action human authorization was CONSUMED by this execution. The historical Stage127 D0 Gate result (item 21) remains `FAIL_M2_DATA_GATE`, unchanged. See `project/stage128/README_STAGE128_M2_D2_GATE_RERUN.md`.
24. `stage127-m2-incremental-evaluation` — **COMPLETE; EXECUTED ONCE UNDER ITS OWN EXPLICIT HUMAN AUTHORIZATION; development-only; NO winner and NO retained block selected.** Both requirements were observed to PASS under the item-23 D2 Gate re-run, and the human supervisor then authorized exactly this one action. The mandatory post-lock D2 eligibility audit was executed first (53 predictor-side comparisons across 6 dimensions; 35 carry |SMD| ≥ 0.10 — descriptive flags only: no row removed, no weighting or matching, no change to D2, the Gate, the sample rule or any model). The paired comparison used the exact three-variable M2 common sample: 539 of 666 development pairs (55 positive / 484 negative; folds 173 / 159 / 332 / 207; validation positives 18 and 10; pooled locked-validation OOF 366 rows with 28 positives). BOTH blocks were REFITTED on identical common-sample training rows and evaluated on identical common-sample validation rows — the original 666-row M1 OOF predictions were NOT reused, and the 666-row M1 results are deliberately NOT compared against these 539-row results. M1 is the retained nine-feature set; M2 is exactly that nested set plus `equity_return_window` (the frozen Gregorian D2 `BOUNDARY_MONTH_ASOF_TRAILING_EQUITY_RETURN` construct), `realized_volatility` and `amihud_illiquidity`; `zero_trade_day_ratio_W` appears only in the eligibility audit. Exactly the three frozen families at their retained Stage126 configurations, five frozen final-OOF seeds, 44 primary predictive model fits — no tuning, no grid search, no feature search, no SMOTE, no early stopping. Observed pooled PR-AUC deltas (M2 − M1, paired company-cluster bootstrap, 2000 replicates, seed 20260724, 2000/2000 valid, percentile 95% CI): logistic +0.0085 [−0.0212, +0.0353]; random forest −0.0073 [−0.0491, +0.0319]; XGBoost +0.0188 [−0.0262, +0.0730]. **All three intervals include zero: the observed development evidence is approximately null.** No new PASS/FAIL threshold was created, no winner was selected, M2 was neither retained nor rejected, and no superiority or causal claim is made. The confirmatory family (`M2_minus_M1`, `M3_minus_M2`, `M4_minus_M3`) is INCOMPLETE: `holm_family_complete = false`, `holm_final_adjustment_deferred = true`. The frozen streaming loader read only the row-identity and split fields required to identify and exclude 346 locked-final-test records; it did not parse, inspect, store, preprocess, fit on, predict from, evaluate, summarize or export any final-test predictor or target value. No model was fit on a final-test row and no full-development refit occurred. M2 development modeling for this comparison WAS executed (44 canonical primary fits); the one-action authorization was CONSUMED, so `m2_incremental_evaluation_authorized` is false again while `m2_modeling_started` is true. M2 is neither retained nor rejected, and M3/M4 were not started. See `project/stage128/m2_incremental_evaluation/README_STAGE127_M2_INCREMENTAL_EVALUATION.md`.
24b. `stage128-m2-retained-block-human-decision` — **COMPLETE; RECORDED ONCE UNDER ITS OWN EXPLICIT ONE-ACTION HUMAN AUTHORIZATION; decision-only, zero scientific execution.** Item 24 reported observed development evidence and deliberately did not answer whether the M2 market block should be retained for the confirmatory programme. The human supervisor has now answered it, weighing the common-sample attrition (127 of 666 parent development rows absent; 13 positive / 114 negative / 53 distinct companies; 0.1906906907), the event composition (28 pooled validation positives on 366 OOF rows), the flagged post-lock D2 eligibility imbalance (53 comparisons, 35 descriptive |SMD| ≥ 0.10 flags), temporal heterogeneity, the wide bootstrap intervals and the disagreement in point-estimate sign across model families. **Recorded outcome: `RETAIN_M2_AS_INTERMEDIATE_CONFIRMATORY_BLOCK`.** M2 stays the intermediate block of the preregistered nested chain M1→M2→M3→M4 and the comparator for a future paired `M3 − M2` evaluation — **only** if the M3 data Gate is separately authorized and passes. The confirmatory family (`M2_minus_M1`, `M3_minus_M2`, `M4_minus_M3`) is unchanged and stays INCOMPLETE with its final Holm adjustment deferred. **This is a retained-block decision, not a superiority decision:** retention is justified as a governance/design decision that preserves the prospectively defined nested confirmatory architecture and avoids post-outcome deletion or redefinition of the `M3 − M2` comparator after observing the M2 results. The observed approximately-null M2 development result is preserved and reported unchanged (logistic +0.008530265112 [−0.021177343686, +0.035281506756]; random forest −0.007313160157 [−0.049131999282, +0.031850216682]; XGBoost +0.018802067544 [−0.026163341118, +0.072970509355]; all three intervals include zero; signs disagree). Retention implies **no** predictive improvement, **no** statistical significance, **no** paper winner, **no** final model, **no** full-development refit and **no** final-test unlock or access. The package was built only by reading already-committed evidence: zero model fits, zero predictions, zero new OOF rows, zero resampling, zero bootstrap/Holm/p-value execution, zero calibration, zero SHAP, zero refits, and zero final-test predictor or target values read; every PR #71 scientific artifact remains byte-identical. The one-action authorization was CONSUMED by this recording. M3 and M4 remain unauthorized and unstarted. See `project/stage128/m2_retained_block_human_decision/README_STAGE128_M2_RETAINED_BLOCK_HUMAN_DECISION.md`.
25. `stage128-m3-macro-data-gate` — **EXECUTED once as a DATA-ADMISSION GATE ONLY; observed result `UNRESOLVED_M3_DATA_GATE`; human review required.** Gated the exact frozen three-variable macro block (`cpi_inflation`, `fx_change_official`, `policy_financing_rate`) for authoritative source, release timing, coverage and low temporal degrees of freedom. **No modeling in the Gate:** 0 model fits, 0 predictions, 0 predictive metrics, 0 M3-versus-M2 comparisons. UNRESOLVED because (a) the frozen contracts do not uniquely determine any candidate's official series or transformation, and (b) no authoritative Central Bank of Iran artifact could be retrieved reproducibly. No candidate was dropped or substituted and no partial block was admitted. `stage128-m3-incremental-evaluation` remains **unauthorized and uncreated**.
25a. `stage128-m3i2-prospective-contract-lock` — **COMPLETE; RECORDED ONCE UNDER ITS OWN EXPLICIT ONE-ACTION HUMAN AUTHORIZATION; contract lock only, zero data and zero execution.** Creates the **prospective** source/definition/statistical contract lock for a **distinct SUPPLEMENTARY** international-macro block **M3I-2** (`intl_cpi_inflation_annual` from World Bank WDI `FP.CPI.TOTL.ZG`, upstream IMF IFS, annual percent, transformation `identity`; `intl_fx_change_official_annual` from WDI `PA.NUS.FCRF`, annual LCU per US dollar, transformation exactly `100 * ln(E_y / E_(y-1))` over two consecutive annual observations from the same vintage, fail-closed to null on missing, non-positive, non-consecutive or cross-vintage inputs), plus a **contingent and UNRESOLVED** M3I-3 financing shell against IMF `IMF.STA:MFS_IR` whose every operational metadata field is `null` and whose `admitted` is false. **M3I-2 is not a substitution, correction or continuation of M3-CBI** (item 25), which is preserved byte-for-byte with `UNRESOLVED_M3_DATA_GATE` and `m3_block_admitted_for_incremental_evaluation: false`; **no M3I block is ever confirmatory M3.** The Data Gate thresholds are INHERITED unchanged (candidate coverage 0.80, block common sample 0.70, ≥5 positives per locked validation window, development-only over the retained-M2 539-row common sample) and the Gate is **NOT_EXECUTED**: every coverage, common-sample and event-count value is recorded as `null`, never as zero. The original confirmatory Holm family (`M2_minus_M1`, `M3_CBI_minus_M2`, `M4_minus_M3_CBI`) is unchanged and INCOMPLETE, and no M3I comparison was inserted into it; a **separate supplementary family** `S1 = M3I_2_minus_retained_M2` / `S2 = M3I_3_minus_M3I_2` is defined but neither hypothesis exists yet. Execution audit, all zero: network requests, data files downloaded, macro observations read, company rows loaded, final-test rows loaded, model fits, predictions, predictive metrics, coverage calculations, Holm calculations. Final test remains locked; M4 remains unauthorized and unstarted; no merge is authorized and the PR is a Draft **stacked on the unmerged PR #73 head** `e6db63fb7d105f0d3a39db101c9e364161c367e9`, never retargeted to `main`. **Corrected after independent audit of PR #74 (contract version `stage128_m3_intl_macro_contract_lock_v1_1`; scope NOT widened).** The audit found the contract selected an archive *vintage* but left two rules operationally non-unique, and both are now locked. (i) **Observation-year selection inside the selected vintage**: an annual period ends on December 31 of the labelled Gregorian observation year, only a period finished strictly before the pair cutoff is eligible, and among eligible years the **maximum** is taken (`selected_observation_tie_breaker = maximum_observation_year`) — never the first/earliest; a fiscal-year label is never a direct WDI year lookup; no eligible observation yields `null`; for FX the maximum eligible year additionally requires `E_y` and `E_(y-1)` present, positive, consecutive, same vintage and same currency denomination and valuation. (ii) **Historical-vintage semantic and currency-unit compatibility**: `historical_archive_metadata_assumed_identical_to_current: false` and `semantic_compatibility_evidence_required_before_value_use: true`, with a per-edition evidence obligation (archive edition identifier, release date/time, Iran economy identity, indicator code, archived series title, annual frequency, unit, calendar-year semantics, raw artifact SHA-256); CPI must remain an annual inflation-RATE series in percent; FX requires one currency denomination and one local-currency valuation convention across the pair with no redenomination or unit break; any mismatch is `null_and_invalid_for_coverage`, an unverified vintage never counts towards coverage, and no alternative series may be tried after a mismatch. Compatibility verification status in this action: `NOT_EXECUTED` — zero archive editions downloaded, zero observation years selected. The correction retrieved no data, executed no Gate, started no modeling, advanced no pointer and authorized no merge. **Topology aligned after the predecessor merge (contract version `stage128_m3_intl_macro_contract_lock_v1_2`; GOVERNANCE ONLY).** PR #73 was merged into `main` by merge commit `b94f73fab99b5c3bc5c55ea7c14736f2bddb516a`, and PR #74 was afterwards retargeted from `stage128-m3-macro-data-gate` to `main`. Two things are now kept strictly apart. (i) **Scientific provenance** is immutable: baseline branch `stage128-m3-macro-data-gate`, baseline commit `e6db63fb7d105f0d3a39db101c9e364161c367e9`, baseline PR #73 — every protected scientific hash is verified against that commit permanently, the branch was **not** rebased, and a merge or retarget never moves the audited baseline. (ii) **Live PR topology** is authoritative for where PR #74 sits: `live_pr_base_branch: main`, `live_pr_base_commit` = `live_main_commit` = `b94f73fa…db516a`, `predecessor_pr_merged: true` with `predecessor_pr_merge_commit: b94f73fa…db516a`, `pr_is_stacked_on_open_predecessor: false`, `retargeted_to_main_after_predecessor_merge_verified: true`, `may_target_main: true`. The pre-merge values are retained under `historical_pre_merge_topology` (`superseded: true`, `describes_current_state: false`); nothing was deleted. The old unconditional "base must never be `main`" rule is replaced by a **state-dependent fail-closed validator**: while the predecessor is open the base must be the predecessor branch with `may_target_main: false`; once the predecessor is merged the base must be `main`, the merge commit must equal live `main`, and the retarget must be verified — and in **both** states `live_pr_is_draft: true`, `live_pr_merged: false` and `merge_authorized: false`. **No scientific content changed**: candidate identity, transformations, Data Gate thresholds, Holm families, missing-value rules, point-in-time rules and the two audited corrections (maximum eligible completed Gregorian observation-year selection; historical-vintage semantic/currency compatibility) are byte-for-byte intact, and the alignment retrieved no data, executed no Gate, started no modeling, advanced no pointer and authorized no merge. See `project/stage128/m3_intl_macro_contract_lock/README_STAGE128_M3_INTL_MACRO_CONTRACT_LOCK.md`.
25b. `stage128-m3i2-official-source-evidence-capture` — **COMPLETE; RECORDED UNDER ITS OWN EXPLICIT ONE-ACTION HUMAN AUTHORIZATION (695 UTF-8 bytes, SHA-256 `eb0230b0…d95b06`, 2026-08-03), which was CONSUMED by the recording; EVIDENCE ACQUISITION ONLY, zero joins and zero execution.** Captured official World Bank WDI and IMF evidence for the merged M3I-2 contract from a single controlled session against official hosts only (`datatopics.worldbank.org`, `databank.worldbank.org`, `data.imf.org`), HTTPS-only, descriptive User-Agent, ≤3 attempts per request: **21 objects, 21 successful, 1,066,295,643 raw bytes retained and hashed**, packaged as the external bundle `papermali_stage128_m3i2_official_source_evidence_bundle_v1.zip` (1,066,004,147 bytes, SHA-256 `4f47586803d9578024e1be88cc353c59be2fa7d8b0ca4f8e3f6aa5d81e9b481c`, 24 members). Development cutoffs were bound to the single frozen surface `project/stage128/stage128_m2_d2_development_features.csv` (field `pair_cutoff_date`, membership `in_three_variable_common_sample`), reading only pair identity, target year and cutoff — no target, financial, market or macro column, and no final-test directory. **37 unique development cutoffs over 539 pairs (an input-integrity count, never coverage).** **110 archive editions were discovered** from the official listing and **16 were captured and held**, yielding **1,878 locked-series rows** for Iran (`IRN`) restricted to `FP.CPI.TOTL.ZG` and `PA.NUS.FCRF` only. **No edition acquired a verified release date: `release_date_verified` is False for all 110 discovered and for all 16 required editions, editions with a verified `available_at` = 0, and every row carries `release_available_at_derivation_status: UNRESOLVED_FILENAME_DATE_TOKEN_NOT_VERIFIED_AS_RELEASE_DATE`.** A filename date token is **not** a release date, and no official source explicitly states an edition release date (`release_date_explicitly_stated_by_official_source` is False for all 110); the 66/44 split in `edition_date_token_exact` is only the exactness of the filename token and was never a day-precision release date. **Semantic compatibility is 16 CPI PASS / 0 UNRESOLVED / 0 FAIL_INTEGRITY and 0 FX PASS / 16 FX UNRESOLVED / 0 FAIL_INTEGRITY** — the archived metadata names no Iranian local-currency denomination and cannot establish that no redenomination or unit break falls across the pair, so FX semantic continuity stands as an **independent, still-open blocker**. **The evidence status is `UNRESOLVED_OFFICIAL_SOURCE_EVIDENCE`**: `cutoffs with a verified pre-cutoff edition = 0 of 37`, every cutoff carrying `selection_reason: NO_VERIFIED_PRE_CUTOFF_EDITION`, so **all 37 cutoffs and all 539 development pairs are UNRESOLVED** — never zero coverage and never an observed failure. The Data Gate is `NOT_EXECUTED` and `m3i2_admitted` is false. The independent bundle integrity audit returned `INDEPENDENT_BUNDLE_INTEGRITY_AUDIT_PASS`, which is an **integrity** result only: it does **not** resolve the historical-vintage problem, does not create a verified release date and does not resolve FX continuity. The IMF financing decision is `NO_EXACT_CANDIDATE_IDENTIFIED_UNRESOLVED_METADATA_LOCK`; M3I-3 stays `UNRESOLVED_METADATA_LOCK` and the merged contract's null fields were **not** populated. The merged contract package `project/stage128/m3_intl_macro_contract_lock/**` is byte-identical to `cf23771a…0647ff`. Execution audit, all zero: company macro joins, feature materializations, coverage calculations, Data Gate executions, model fits, predictions, predictive metrics, Holm calculations, final-test rows read. **Evidence capture is not admission and does not authorize the Data Gate.** See `project/stage128/m3i2_official_source_evidence_capture/README_STAGE128_M3I2_OFFICIAL_SOURCE_EVIDENCE_CAPTURE.md`.

25c. `stage128-m3i2-final-official-documentary-recovery-initiation` — **COMPLETE; RECORDED UNDER ITS OWN EXPLICIT ONE-ACTION HUMAN AUTHORIZATION (252 UTF-8 bytes, SHA-256 `a1878df0…9e97`), which was CONSUMED by the recording; INITIATION ONLY, zero data and zero execution.** A final, bounded attempt to recover **official documentary** evidence for the two remaining M3I-2 blockers — (i) archive release availability (is there an official release calendar, version history, edition log or publication-date record, and what does an archive filename date token actually mean?) and (ii) historical FX semantic continuity for `PA.NUS.FCRF` / `IRN` (denomination, valuation convention, period-average construction, redenomination, unit break, consecutive-year comparability) — plus preparation of exactly ONE official World Bank Data Help Desk inquiry with two public attachments. Bounds honoured: **14 documentary GET requests of a maximum of 20**, World Bank Group hosts only, **0 archive ZIP downloads and 0 redownloads**, and **0 repeats of any URL in the prior capture manifest** (a fail-closed guard refuses duplicates, ZIPs, non-official hosts and non-HTTPS). Every retained response is hashed. **Bounded-search outcome `NO_NEW_DOCUMENTARY_EVIDENCE_IN_BOUNDED_SEARCH`:** the official WDI release notes exist only from December 2024 onward; the official Data Updates and Errata page announces dated **database** updates rather than archive-edition publications, and several announced dates differ from the archive filename tokens; the complete official Help Desk article index contains no archive-release and no Iranian-denomination article; the `PA.NUS.FCRF` indicator page confirms only the series title and the IMF IFS source. **Rule D therefore stands for every edition: `available_at = null`, `release_date_verified = false`**, `filename_token_is_release_evidence: false` and no unproven previous-month fallback was used. **Submission status `HUMAN_SUBMISSION_REQUIRED`:** the Help Desk exposes no public support form and a ticket requires a signed-in account, so automation prepared the inquiry (body SHA-256 `dd82929f…e8b5`) and its attachments instead of submitting; **no ticket was opened, none was invented, no credential was used and no human-verification step was bypassed**. The stopping rule is locked prospectively: initial inquiry maximum 1, waiting period 10 business days (Mon-Fri, submission day excluded), follow-up maximum 1 and only under a separate explicit authorization, automatic follow-up forbidden, response ingestion and adjudication only in a separate action, terminal status `UNRESOLVED_AFTER_FINAL_OFFICIAL_INQUIRY`. Resolving only one blocker never admits M3I-2. Execution audit, all zero: company macro joins, feature materializations, coverage calculations, Data Gate executions, model fits, predictions, predictive metrics, bootstrap executions, Holm calculations, target values read, final-test rows read. M3I-2 stays `UNRESOLVED_OFFICIAL_SOURCE_EVIDENCE` and unadmitted; M3-CBI, M3I-3 and the Holm families are untouched; **M3-LAG-WDI-EXPLORATORY was `NOT_LOCKED` at the time of this item** (see the note above on the quarantined local draft; superseded at item 25h); the Final Test stays locked, M4 unauthorized and no merge is authorized. See `project/stage128/m3i2_final_official_documentary_recovery/README_STAGE128_M3I2_FINAL_OFFICIAL_DOCUMENTARY_RECOVERY.md`.

25d. `stage128-m3i2-final-official-inquiry-human-submission` — **COMPLETE; the submission was performed by a HUMAN SUPERVISOR and RECORDED under its own explicit one-action human authorization (95 UTF-8 bytes, SHA-256 `4562e480…7978`), which was CONSUMED by the recording; SANITIZED RECORDING ONLY, zero data and zero execution.** The prepared inquiry was submitted **exactly once** through the official World Bank Data Help Desk *Contact support* channel, category *Data Compilation Methodology*, subject `Request for official historical WDI archive release dates and PA.NUS.FCRF unit continuity for Iran`, canonical body SHA-256 `dd82929f…e8b5`, with both public attachments (`5c473948…3a35b`, `2cc118c2…76cf94`) visibly selected before submission. Automation submitted nothing, used no credential, accessed no personal mailbox and bypassed no human-verification step. **An acknowledgement was received and it is NOT a substantive World Bank response:** the confirmation page reported the message as received and the confirmation e-mail promised a later reply by a data support specialist. **No ticket id was supplied** — none was displayed on the confirmation page or in the e-mail, so `ticket_id_present: false`, `ticket_id_redacted: null`, `ticket_id_sha256: null`, `ticket_id_fabricated: false`. **The UTC instant is unresolved and was never guessed:** the UI showed `2026-08-06 14:03` with no timezone, so `submission_timestamp_utc: null` under `UNRESOLVED_CONFIRMATION_UI_DID_NOT_DISPLAY_TIMEZONE`, and only the official displayed calendar date `2026-08-06` drives the waiting-period rule. Two claims are deliberately kept weak: the canonical body was `CANONICAL_BODY_VISUALLY_CONFIRMED_NOT_RAW_BYTE_VERIFIED` and the attachments were `attachments_selected_before_submission: true` but `attachments_server_confirmation_enumerated: false`. **The raw confirmation is external only:** the web confirmation copy (`14060eef…31f6`, 631,880 bytes) and the two e-mail copies (`8841e6ab…1e85`, 383,457 bytes; `dd95e549…6ca9`, 339,376 bytes) are each `stored_outside_repository: true`, `committed_to_git: false`, `contains_personal_information: true`; only hashes and byte sizes are committed and no PII recovery from the screenshots was attempted. **The waiting period is ACTIVE:** business day 1 `2026-08-07`, business day 10 `2026-08-20`, completion `2026-08-20`, earliest possible follow-up `2026-08-21`; `automatic_follow_up_forbidden: true`, `follow_up_attempted: 0`, `follow_up_authorized_now: false`, and response ingestion/adjudication remain unauthorized. Execution audit, all zero: new documentary searches, WDI archive downloads, network requests, coverage calculations, feature materializations, Data Gate executions, model fits, predictions, predictive metrics, final-test rows read. M3I-2 stays `UNRESOLVED_OFFICIAL_SOURCE_EVIDENCE` and unadmitted; no merge is authorized and the PR is a Draft. See `project/stage128/m3i2_final_official_inquiry_human_submission/README_STAGE128_M3I2_FINAL_OFFICIAL_INQUIRY_HUMAN_SUBMISSION.md`.

25e. `stage128-m3i2-official-source-evidence-review` — **HISTORICAL / SUPERSEDED POINTER ONLY; NOT AUTHORIZED; NOT the current next action.** This pointer was recorded under an earlier interpretation of the PR #75 evidence and is retained only as historical roadmap state. That interpretation has since been corrected against the merged package's own artifacts: **no WDI archive edition has a verified release date** (`release_date_verified` False for all 110 discovered and all 16 required editions), **0 of 37 cutoffs has a verified pre-cutoff edition**, and **all 37 cutoffs and all 539 development pairs remain unresolved** — so the narrower gap this pointer was written to review never existed as stated. It authorizes no review, no Data Gate and no modeling. The human submission `stage128-m3i2-final-official-inquiry-human-submission` (item 25d) has since been performed once and recorded, and the current pointer is now `stage128-m3i2-final-official-inquiry-response-ingestion` (item 25f), which is also **not authorized**.

25f. `stage128-m3i2-final-official-inquiry-response-ingestion` — **POINTER ONLY; NOT AUTHORIZED.** Only if a **substantive** World Bank response actually arrives. It would ingest and adjudicate that response against the two open blockers under its own explicit human authorization. The acknowledgement already received is **not** such a response. It executes no Data Gate, admits nothing and is not authorized by item 25d.

25g. `stage128-m3i2-final-official-inquiry-one-follow-up` — **CONDITIONAL POINTER ONLY; NOT AUTHORIZED.** At most ONE follow-up may ever exist, it may not be sent before **2026-08-21** (the day after the 10-business-day waiting period completes on 2026-08-20), and then only if the response is still absent or insufficient. Reaching that date creates **eligibility only**, never authorization: a follow-up still requires a separate explicit human authorization. Automatic follow-up is forbidden.
25h. `stage128-m3-lag-wdi-exploratory-contract-lock` — **COMPLETE; TRACK B; RECORDED UNDER ITS OWN NEW EXPLICIT ONE-ACTION HUMAN AUTHORIZATION (158 UTF-8 bytes, SHA-256 `0c1e1049…995b`), which was CONSUMED by this contract lock; CONTRACT LOCK ONLY, zero retrieval and zero execution.** The M3-LAG-WDI-EXPLORATORY path is now activated **in parallel** with the still-active World Bank inquiry, with its authoritative contract frozen **before any data retrieval**. Status `AUTHORITATIVE_CONTRACT_LOCKED_PRE_RETRIEVAL`. Locked: the exploratory-only role (`supplementary_exploratory_robustness_block` — not confirmatory M3, not a replacement or repair of M3-CBI, not a continuation of M3I-2, not proof of point-in-time availability, not able by itself to select the paper winner); **exactly two** additional macro features, `intl_cpi_inflation_lag1_wdi` (`FP.CPI.TOTL.ZG`, `IRN`, observation year `t-1`, identity — predictor year 2019 → CPI 2018, no same-year `t` observation) and `intl_fx_change_official_lag1_wdi` (`PA.NUS.FCRF`, `IRN`, `FX_LAG1_t = 100 * ln(E_(t-1) / E_(t-2))`, both observations present, numeric, strictly positive and consecutive, otherwise NULL, with `PA.NUS.ATLS`, free-market, unofficial, aggregator-derived rates and post-hoc alternative transformations explicitly forbidden); no third macro feature, no financing-rate feature, no indicator search, no imputation; the retained-M2 **539-row** development parent sample (never the 666-row M1 sample), **12** M2 features versus **14** M3-LAG-WDI features, complete cases required for **both** new features, and **both** models refitted on the identical resulting common sample; the **inherited** Gate thresholds (candidate coverage `>= 0.80`, block coverage `>= 0.70`, `>= 5` positives in each locked validation window, development-only, all observed coverage values `null` and **not** zero); and the three already-frozen model families (logistic regression, random forest, XGBoost) in their retained configurations with no retuning, no grid search, no hyperparameter search and no model-family search. The one-year lag is a **conservative temporal-separation design only** — the future retrieval uses the then-current/latest **revised** WDI, so historical-vintage and point-in-time availability are explicitly **not** claimed, and the M3I-2 historical-vintage logic is **not** reused to claim point-in-time validity. The comparison `M3_LAG_WDI_minus_retained_M2` lives in a **separate** exploratory family and is **never** inserted into the confirmatory Holm family (`M2_minus_M1`, `M3_CBI_minus_M2`, `M4_minus_M3_CBI`); a positive **or** negative exploratory result may not rewrite the main confirmatory conclusion. Execution audit, all zero: World Bank API requests, WDI downloads, WDI value inspections, company-row macro joins, feature materializations, coverage calculations, Data Gate executions, model fits, predictions, predictive metrics, bootstrap executions, Holm calculations, SHAP executions, final-test rows read. A future Data Gate PASS would be **data admission only** and would authorize no modeling; retrieval, the Gate and any predictive evaluation each need their own separate explicit human authorization. The earlier quarantined local draft was left untouched, not promoted and not committed. See `project/stage128/m3_lag_wdi_exploratory_contract_lock/README_STAGE128_M3_LAG_WDI_EXPLORATORY_CONTRACT_LOCK.md`.

**TRACK B FUTURE SEQUENCE — RETRIEVAL, THE DATA GATE AND MODELING ARE THREE SEPARATE ACTIONS.** An authorization boundary only exists where an action boundary exists, so these are never bundled and never share an identity. **Retrieving is not Gating, and Gating is not modeling.** An authorization to retrieve is **not** an authorization to execute the Data Gate (`m3_lag_wdi_retrieval_authorization_implies_gate_authorization: false`); a combined retrieval-and-Gate action is forbidden (`m3_lag_wdi_combined_retrieval_and_gate_action_permitted: false`); and a Gate PASS is **data admission only** (`m3_lag_wdi_gate_pass_is_data_admission_only: true`, `m3_lag_wdi_gate_pass_authorizes_modeling: false`). Every one of items 25i–25l is a **pointer**, and a pointer is **never** an authorization.

25i. `stage128-m3-lag-wdi-exploratory-data-retrieval` — **COMPLETE; TRACK B; RECORDED UNDER ITS OWN NEW EXPLICIT ONE-ACTION HUMAN AUTHORIZATION (125 UTF-8 bytes, SHA-256 `b409e0a5…a604`), which was CONSUMED by this retrieval; RETRIEVAL ONLY, zero interpretation.** The two locked WDI series were acquired from the official World Bank WDI API over HTTPS (`api.worldbank.org`): `FP.CPI.TOTL.ZG` and `PA.NUS.FCRF`, both for `IRN`, **2 API requests, 2 successful responses, 2 raw artifacts retained, 32,287 raw bytes**. Raw payloads are retained **outside the repository**; only their byte counts and SHA-256 digests are committed (`raw_payloads_committed_to_git: 0`). **Acquiring bytes is not admitting data.** The payload was **never decoded or parsed** (`payload_json_decoded: false`, `wdi_observations_read: 0`): reading what is inside the retained bytes is item 25j, which is NOT authorized. Execution audit, all zero: WDI value inspections, alternative/proxy indicators searched or retrieved, coverage calculations, candidate and block coverage evaluations, Data Gate executions, admission decisions, company-row macro joins, feature materializations, FX transformation calculations, common-sample constructions, model fits, predictions, predictive metrics, bootstrap executions, Holm calculations, SHAP executions, tuning runs, **final-test rows read**. No indicator search, no substitution and no post-hoc alternative series: the retrieval layer structurally cannot build a URL for any code outside the two locked ones or any country outside `IRN`. The authoritative contract is unchanged and still `AUTHORITATIVE_CONTRACT_LOCKED_PRE_RETRIEVAL`; the retrieval authorization is **consumed** and is **not** reusable for item 25j, 25k or 25l. Track A is untouched. See `project/stage128/m3_lag_wdi_exploratory_data_retrieval/README_STAGE128_M3_LAG_WDI_EXPLORATORY_DATA_RETRIEVAL.md`.

25j. `stage128-m3-lag-wdi-exploratory-post-retrieval-audit` — **COMPLETE; TRACK B STEP C; EXECUTED ONCE UNDER ITS OWN NEW EXPLICIT SINGLE-USE HUMAN AUTHORIZATION, WHICH WAS CONSUMED BY THIS EXECUTION; AUDIT ONLY, ZERO ADMISSION.** This is the first and only action permitted to DECODE the payloads acquired by item 25i, and the only question it answers is what the retained evidence actually contains. Identity was proven on the raw bytes **before** decoding — byte count and SHA-256 re-verified against the committed manifest, itself anchored to the immutable Zenodo record `10.5281/zenodo.21844636` — and the audited evidence was **not** modified. Result `PASS_WITH_MATERIAL_FINDINGS`: both payloads are authentic, complete (single page, 66 annual observations each, no calendar gaps, no duplicate years), schema-conformant to the World Bank v2 envelope, and carry exactly the contract-locked indicator and country. 132 WDI observations were read. **Three material findings are recorded and must not be summarised away.** (i) `PA.NUS.FCRF` carries no value for 2024 and 2025, capping the FX feature at predictor year 2024; since the contract requires **both** features complete, the series-level ceiling for the block is predictor years **1962–2024**, bound by `PA.NUS.FCRF` (`FP.CPI.TOTL.ZG` alone would reach 2026). (ii) The official exchange rate is repeated **unchanged** across the most recent usable years, so the contract-locked transform `100 * ln(E_(t-1)/E_(t-2))` is defined but **identically zero** for predictor years 2021–2024 — the last four usable predictor years carry a complete but information-free FX feature. Completeness there does not imply information, and a coverage count alone would not reveal this. (iii) The WDI vintage is a revision marker (`lastupdated 2026-07-13`), not evidence of what was published at any past moment; no point-in-time or historical-vintage availability is established, consistent with the contract. **Reading is not admitting.** Execution audit, all zero: new World Bank requests, coverage calculations, candidate/block coverage evaluations, coverage-threshold comparisons, Data Gate executions, admission decisions, company-row macro joins, feature materializations, common-sample constructions, model fits, predictions, predictive metrics, bootstrap executions, Holm calculations, SHAP executions, Final Test rows read. Every availability statement above is **series level**, never sample level: 0 company rows were touched. Translating series availability into per-row coverage, comparing it to the inherited thresholds and deciding admission is the **Data Gate**, item 25k. A PASS here is **not** a Gate authorization (`post_retrieval_audit_pass_is_gate_authorization: false`) and **not** an admission (`post_retrieval_audit_pass_is_admission: false`). The retrieval authorization stays historical and consumed (`retrieval_was_authorized: true`, `retrieval_authorized_now: false`). The Track B pointer now advances to `stage128-m3-lag-wdi-exploratory-data-gate`, which remains **unauthorized** and unexecuted. See `project/stage128/m3_lag_wdi_exploratory_post_retrieval_audit/README_STAGE128_M3_LAG_WDI_EXPLORATORY_POST_RETRIEVAL_AUDIT.md`.

25k. `stage128-m3-lag-wdi-exploratory-data-gate` — **COMPLETE; TRACK B STEP D; EXECUTED ONCE UNDER ITS OWN NEW EXPLICIT SINGLE-USE HUMAN AUTHORIZATION, WHICH WAS CONSUMED BY THIS EXECUTION; DATA ADMISSION ONLY, ZERO MODELING.** The development-only Data Gate was executed against the thresholds inherited and frozen at item 25h — candidate coverage `>= 0.80`, block common-sample coverage `>= 0.70`, `>= 5` positive evaluable outcomes in each locked validation window — re-read at run time from the locked Gate contract and its inheritance source, unchanged by this action (`thresholds_changed_to_obtain_result: false`, `criteria_weakened: false`). Denominator: the exact retained-M2 **539-row** development common sample, derived programmatically and reconciled against the committed M2 join audit, with 0 final-test rows. **Result `PASS_M3_LAG_WDI_DATA_GATE`** — CPI 539/539 (1.0000), FX 539/539 (1.0000), block common sample 539/539 (1.0000), positive evaluable 18 and 10 in the two locked validation windows, **0 rows excluded, 0 features excluded, 0 years excluded**, no imputation and no alternative indicator. **The block is formally ADMITTED, and that admission is DATA ADMISSION ONLY.** It authorizes **no** model fit (`gate_pass_authorizes_modeling: false`), makes **no** claim that either feature is informative (`gate_pass_is_information_content_claim: false`) and does **not** unlock the Final Test. **The item-25j findings are preserved, not superseded.** The Gate report distinguishes five things explicitly so a coverage PASS can never be misread: (A) syntactic availability/coverage; (B) which pre-defined thresholds were satisfied; (C) the step C information-content limitation — the FX log-ratio is defined but **identically zero for predictor years 2021–2024**; (D) whether that limitation changes the formal decision under the PRE-EXISTING rules — it does **not**, because the locked contract contains no zero-change or information-content admission criterion and none was invented, and because the development rows land on Gregorian predictor years no later than 2020 under either admissible calendar convention, so the degenerate window lies outside the sample (**0** development rows are zero-change); and (E) whether it survives as a scientific limitation — it **does**, together with the `PA.NUS.FCRF` 2024–2025 nulls that cap the jointly constructible ceiling at predictor year 2024 and the WDI revision-marker (not point-in-time) vintage semantics. **One new material limitation was recorded by this step:** the locked contract does not fix the Jalali→Gregorian mapping for `predictor_year_t`. The Gate refused to invent it, computed every row's status under **both** admissible conventions (`jalali + 621` and `jalali + 622`) and found them identical, so the verdict is well-defined despite the gap — but feature **values** are not invariant, so **no feature-value table was materialized** and the mapping must be human-locked before any modeling table can exist. Execution audit, all zero: new World Bank requests, new payloads retrieved, alternative indicators searched or retrieved, feature-value tables materialized, model fits, predictions, predictive metrics, bootstrap executions, Holm calculations, SHAP executions, tuning runs, cross-validation runs, model selections, **Final Test rows read**. Retained and deposited evidence were not modified; item 25j was not rerun and its accepted result is unchanged. The Gate authorization is **consumed** and is **not** reusable (`m3_lag_wdi_data_gate_authorized_now: false`), and it does **not** propagate to item 25l. See `project/stage128/m3_lag_wdi_exploratory_data_gate/README_STAGE128_M3_LAG_WDI_EXPLORATORY_DATA_GATE.md`.

25k-bis. `stage128-m3-lag-wdi-exploratory-calendar-mapping-lock` — **COMPLETE; EXECUTED ONCE UNDER ITS OWN NEW EXPLICIT SINGLE-USE HUMAN SCIENTIFIC DECISION, WHICH WAS CONSUMED BY THIS EXECUTION; TIMING DECISION ONLY, ZERO MODELING.** Item 25k's Gate PASS is invariant to the two admissible Jalali→Gregorian mappings, but the feature **values** are not — so the mapping had to be locked before any modeling feature table could exist. **Locked rule: `predictor_year_t = jalali_fiscal_year_t + 621`** (`jalali_fiscal_year_t_plus_621`), i.e. the Gregorian year in which the Jalali year BEGINS. `fiscal_year_t` was established to label **the Jalali year in which the accounting period ENDS** (539/539 agreement, by inverting the Stage125 Part 3C four-Jalali-month regulatory lag over the committed cutoffs). CPI uses observation year `t-1`; FX uses `t-1` and `t-2`; the binding observation year is `t-1`. Neither observation rule was changed. **`+622` was rejected on timing, not on outcomes.** Applying the frozen `G07 no_future_or_target_year_information` rule, the binding `t-1` observation year must be COMPLETE at the prediction cutoff. `+622` fails that for **22 of 539** development rows spread across **every** fiscal-year cohort 1392–1398 (worst case: the required calendar year completes **131 days after** the cutoff). `+621` fails it for **0 of 539**, with a minimum margin of **234 days**. Development predictor years are therefore **2013–2019** and binding observation years **2012–2018**. **The lock is fail-closed, not declarative:** the runner recomputes this evidence from committed bytes on both `--execute` and `--check` and refuses to write a lock for any offset admitting a single timing violation, so `+622` is **structurally unlockable** — swapping a constant is not enough. The recomputation needs only `fiscal_year_t` and `pair_cutoff_date` plus integer arithmetic: no calendar library, no feature value, no outcome label, no Final Test row. **Selection was independent of model performance** (`selection_used_model_performance: false`, `selection_used_coverage_comparison: false`, `selection_used_feature_values: false`); the justification stands unchanged if `+622` later produced better predictive numbers, since the 22 violating rows are exactly where a look-ahead advantage would come from. **What the lock does NOT establish:** point-in-time WDI availability remains **unproven** — the values are current/latest revised WDI, the one-year lag remains conservative temporal separation only, the FX 2021–2024 degeneracy and the `PA.NUS.FCRF` 2024–2025 missingness both stand. The authoritative pre-retrieval contract is **amended, not edited** (its historical unlocked state is retained), following the Stage125 Part 3C superseding pattern. Item 25k was not rerun and its result is unchanged. Execution audit, all zero: feature-value tables materialized, feature values computed, model fits, Gate executions, new World Bank requests, **Final Test rows read**. The lock authorizes **nothing** downstream: it is not modeling authorization, not permission to build a feature table, and does not propagate to item 25l. See `project/stage128/m3_lag_wdi_exploratory_calendar_mapping_lock/README_STAGE128_M3_LAG_WDI_EXPLORATORY_CALENDAR_MAPPING_LOCK.md`.

25l. `stage128-m3-lag-wdi-exploratory-incremental-evaluation` — **COMPLETE; EXECUTED ONCE UNDER ITS OWN NEW EXPLICIT SINGLE-USE HUMAN AUTHORIZATION, WHICH WAS CONSUMED BY THIS EXECUTION; DEVELOPMENT-ONLY; SUPPLEMENTARY EXPLORATORY RESULT, ZERO FINAL TEST.** The one authorized execution of the already-frozen step-E contract, and the first Track B action permitted to materialize a feature **value** table or fit a model. It made **no new scientific design choice**: the calendar mapping (`predictor_year_t = jalali_fiscal_year_t + 621`), the two admitted features, the complete-case policy, the three retained model configurations, the validation windows, the canonical metric definitions, the seed policy and the paired bootstrap machinery were all frozen before it ran, and every one of them is re-read from committed bytes at run time so drift fails closed. **Sample:** the frozen complete-case rule removed nothing (both features are constructible for all 539 admitted rows), so the modeling sample is exactly the retained-M2 development common sample — **539 rows, 55 positive / 484 negative, 108 companies, 366 pooled out-of-fold rows carrying 28 positives**, validation positives 18 and 10, **0** exclusions outside the frozen rule, **0** final-test rows. Both blocks were evaluated on that identical sample; the comparator was **refit**, never imported, and the published 666-row M1 results were **not** used as the comparator. Because the sample is identical, the refit M2 reproduces the committed retained-M2 metrics **exactly** (108 values compared, 0 mismatches) — a reconciliation, not a substitution. **Architecture:** retained M2 = **12** features versus M3-LAG-WDI = **14** (the same 12 plus exactly `intl_cpi_inflation_lag1_wdi` at observation year `t-1`, identity, and `intl_fx_change_official_lag1_wdi` = `100 * ln(E_(t-1) / E_(t-2))`), nested, in **44** primary predictive fits (2 blocks x 2 folds x [1 logistic + 5 RF + 5 XGB]). No third macro feature, no feature search, no selection, no substitution, no imputation, no retuning, no grid search, no model-family search, no SHAP (the frozen contract does not require one), and **no same-year `t` observation was read**. **Result — `E1`, in the separate exploratory family `M3_LAG_WDI_EXPLORATORY_SUPPLEMENTARY`, labelled `supplementary_exploratory_robustness_only`:** `E1_NULL_NO_DETECTABLE_INCREMENTAL_CONTRIBUTION`. Paired pooled-OOF PR-AUC differences (M3-LAG-WDI minus retained M2) were **+0.000862** (logistic), **-0.002720** (random forest) and **+0.002749** (XGBoost); all three 95% paired company-cluster bootstrap intervals (2000 replicates, seed 20260724, 2000/2000 valid) **include zero**. Adding the two lagged WDI macro features changed nothing detectable in **discrimination**. **A secondary-metric signal is reported, not hidden behind that null:** the paired **Brier score** interval excludes zero in two of three families, favouring M3-LAG-WDI (logistic **-0.004600** [-0.006147, -0.003066]; random forest **-0.001375** [-0.002229, -0.000566]; lower Brier is better). Brier measures **calibration**, not ranking, so a better Brier alongside an unchanged PR-AUC reads coherently as the macro features shifting the probability **level** within a year without improving the company-level **ordering** inside it — exactly what year-constant features would be expected to do. It is **not** evidence of incremental discriminative value, it stays exploratory and supplementary, and it is **not** a confirmatory claim. **Interpretation is bounded.** A null result is not proof of exactly zero effect: with 28 pooled out-of-fold positives the intervals are wide relative to the observed differences. Both features are **national annual** series, so within a predictor year every company shares one value — across the sample they take only 7 distinct values each, one per predictor year 2013–2019 — and because the temporal folds make training and validation years disjoint by construction, every value the block sees in a validation window is one it never saw in training. The block can shift or rescale predictions within a validation year but cannot contribute company-level discrimination inside it, which is what the ranking metrics measure. That is a structural property of the design, not an artefact of the observed result. **Every limitation survives:** point-in-time WDI availability remains **UNPROVEN** (`lastupdated` is a revision marker; lagging revised WDI does not create point-in-time data), the FX log-ratio is identically **zero for predictor years 2021–2024** (outside this development sample, real for any extension), and `PA.NUS.FCRF` remains missing for observation years **2024–2025**. None of these is resolved by this action and none would have been erased by a favourable result. **Nothing else moved.** The block's role stays `supplementary_exploratory_robustness_block` — not confirmatory M3, not a replacement or repair of M3-CBI, not a replacement for M3I-2, not historical-vintage or real-time WDI, never in the confirmatory Holm family, never able to select the paper winner. The confirmatory Holm family (`M2_minus_M1`, `M3_CBI_minus_M2`, `M4_minus_M3_CBI`) was **not executed and not modified**, no confirmatory superiority claim was made, and the main confirmatory conclusions are exactly what they were before. Counters: **0** World Bank requests, **0** step C reruns, **0** step D / Gate reruns, **0** calendar-lock reruns, **0** retuning or grid searches, **0** SHAP runs, **0** Holm calculations, **0** paper-winner selections, **0** Final Test rows read. QC: 98 assertions, 0 failed. The step-E authorization is **consumed and non-reusable** (`m3_lag_wdi_modeling_was_authorized: true`, `m3_lag_wdi_modeling_authorized: false`). The locked Track B sequence is now exhausted: `m3_lag_wdi_next_action_id: human_decision_required`, `m3_lag_wdi_next_action_authorized: false`. PR #79 stays a **Draft** (not ready-for-review, not merged), the Final Test stays **hard locked**, and M4 stays **unauthorized**. See `project/stage128/m3_lag_wdi_exploratory_incremental_evaluation/README_STAGE128_M3_LAG_WDI_EXPLORATORY_INCREMENTAL_EVALUATION.md`.

25m. `stage128-m3i2-track-a-waiting-termination-and-m3-disposition` — **COMPLETE; RECORDED UNDER AN EXPLICIT HUMAN GOVERNANCE DECISION (2026-08-08), NOT A ONE-ACTION EXECUTION AUTHORIZATION; DECISION RECORDING ONLY, ZERO DATA AND ZERO EXECUTION.** The human supervisor decided to voluntarily terminate the Track A waiting period early rather than wait for the previously locked completion date **2026-08-20**, and separately froze the final research disposition of Track B (M3-LAG-WDI-EXPLORATORY). **This is not recorded as "the World Bank will not respond"** — that claim is unproven and explicitly not made (`world_bank_will_not_respond_claim_made: false`). What is recorded, verbatim: *"As of 2026-08-08, no response resolving the point-in-time availability question had been obtained, and the human researcher elected to terminate the waiting period and adjudicate the M3-LAG-WDI evidence using the currently available evidence."* **No further Track A action is authorized:** no World Bank follow-up, no repeated request, no additional WDI API or archive retrieval, and no attempt to infer, manufacture or backfill a historical WDI release date. **Historical point-in-time availability of the relevant WDI observations remains UNVERIFIED** with currently available evidence — recorded as an evidence limitation, not a blocking task. **A future unsolicited World Bank response does not automatically reopen or rerun M3**; using it for anything would require a separate, new, explicit human decision (`unsolicited_future_world_bank_response_auto_reopens_m3: false`). **M3-LAG-WDI's final research disposition is `SUPPLEMENTARY_EXPLORATORY_ONLY`** — it is not, and this decision does not promote it into, the main confirmatory model (`m3_lag_wdi_promoted_to_confirmatory_model: false`), and it stays outside the confirmatory Holm family (`M2_minus_M1`, `M3_CBI_minus_M2`, `M4_minus_M3_CBI`). **Item 25l's already-completed result is preserved exactly**, byte-for-byte, and re-verified against the committed step-E artifact by this recording, not merely restated: `E1_NULL_NO_DETECTABLE_INCREMENTAL_CONTRIBUTION`; paired PR-AUC deltas logistic **+0.000862** `[-0.028237, +0.032186]`, random forest **-0.002720** `[-0.029157, +0.011924]`, XGBoost **+0.002749** `[-0.007437, +0.014554]` (all three 95% intervals include zero); secondary Brier deltas logistic **-0.004600** `[-0.006147, -0.003066]` and random forest **-0.001375** `[-0.002229, -0.000566]` (calibration only, non-confirmatory). **M4 remains NOT AUTHORIZED. The Final Test remains LOCKED, `final_test_rows_read: 0`. The confirmatory Holm family is unchanged and unexecuted by this action. No paper winner is selected.** Execution audit, all zero: World Bank requests, WDI API requests, WDI archive downloads, network requests, coverage calculations, feature materializations, Data Gate executions, model fits, predictions, predictive metrics, bootstrap executions, Holm calculations, SHAP executions, Final Test rows read. The next action on **both** pointer chains is now the same: `next_research_action_id: human_decision_required`, `next_research_action_scope: no_further_action_is_authorized`, `next_research_action_authorized: false` — superseding the earlier Track A pointer `stage128-m3i2-final-official-inquiry-response-ingestion` (item 25f, retained as history) and confirming the already-`human_decision_required` Track B pointer set at item 25l. A pointer is never an authorization. See `project/stage128/m3i2_track_a_waiting_termination_and_m3_disposition/README_STAGE128_M3I2_TRACK_A_WAITING_TERMINATION_AND_M3_DISPOSITION.md`.

25n. `human-decision-required` — **POINTER ONLY; NOT AN ACTION AND NOT AUTHORIZED.** The literal state item 25m leaves behind: with the Track A waiting period voluntarily terminated and the Track B sequence already exhausted at item 25l, **no next research action is named or authorized** on either pointer chain. This item exists in the ordered list only so the front-matter `next_research_action_id` truthfully names "a human must choose the next action" instead of pointing at a specific action (Track A follow-up, response ingestion, or any Track B step) that is **not** what is actually about to happen. It authorizes nothing, executes nothing, and is not itself a scientific or governance action — it is a placeholder for the fact that the next research action is undetermined pending a separate future human decision.

26. `stage128-m3-incremental-evaluation` — **conditional on Gate pass.** Evaluate M3 vs M2 on the paired M3 common sample. Keep the macro set parsimonious; no searched macro universe.
27. `stage129-m4-governance-data-gate` — Gate each structured audit/governance predictor individually for definition, point-in-time availability, coverage and join quality. **No text modeling.**
28. `stage129-m4-incremental-evaluation` — **conditional on Gate pass.** Evaluate M4 vs M3 on the paired M4 common sample using only admitted structured variables.
29. `stage130-pre-final-design-and-claim-freeze` — choose the retained block/design using development evidence only; freeze exact preprocessing/configurations, comparison family, manuscript claim hierarchy, bootstrap/Holm plan and final-test reporting rules. **No final-test inspection.**
30. `stage131-full-development-refit` — refit the already-frozen retained design on the complete development period only. No tuning, feature changes or final-test access.
31. `stage132-locked-final-temporal-evaluation` — one evaluation on target years 1400–1402 after all design decisions are frozen. Report discrimination, calibration, screening value and uncertainty. **No retuning or model changes after inspection.**
32. `stage133-explainability-and-stability` — descriptive SHAP/coefficient and performance stability analysis for the retained design; no causal interpretation and no model modification.
33. `stage134-manuscript-reproducibility-and-claim-audit` — generate manuscript tables/figures, reproducibility manifest, SAP-deviation log, limitations/negative-results section, and final Q1/Q2 reporting/risk-of-bias checklist before submission.

## Maintenance tasks

- `repository-driven-ai-handoff` — repository-derived Handoff package. Operational aid only; it does not advance research.
- `stage126-q1q2-lean-governance-reset` — adopt the Stage126+ lean governance standard: scientific locks remain strict, operational test/QC/Handoff/hash coupling is simplified, one live current-state authority is used, and future acceptance gates are proportional to scientific risk.
- `stage126-legacy-validation-boundary-adaptation` — after the lean-governance decision is merged, simplify the legacy M1 live/historical validation mechanics so stale operational assertions cannot block a truthful Part 6 terminal state. **Must not alter Parts 1–5 scientific outputs, M1 primary results, final-test lock, target/sample/split or selected configurations.**
- Historical Stage125 maintenance/decision-lock tasks remain in Git history and their artifacts; they do not define the Stage126+ engineering workflow.

## Stage126+ scientific quality requirements

The roadmap requires the following before a result enters the manuscript:

- explicit sample formation and positive/negative/unknown accounting;
- point-in-time `t -> t+1` predictor timing and leakage control;
- pre-specified temporal development folds and one locked temporal final evaluation;
- common-sample paired comparisons for nested M1→M4 value-add analyses;
- PR-AUC primary; ROC-AUC complementary; Brier, calibration, Recall@10% and Lift@10%;
- 95% uncertainty intervals where estimable and paired ticker-cluster bootstrap deltas for pre-specified block comparisons;
- transparent reporting when low event counts make calibration/inference unstable;
- Holm correction only for the pre-specified inferential family in the SAP;
- no final-test model selection, winner selection, retuning, feature changes or threshold hunting;
- explanation stability checks rather than a single SHAP plot;
- explicit limitations, negative results and generalizability constraints;
- traceability of every manuscript number to a scientific artifact/table-generation output/citable source.

The 1400–1402 holdout is a **locked temporal evaluation set**, not external validation. With approximately 12 primary-target positives, final-test claims remain deliberately conservative.

## Notes on the current transition

The current canonical state on this branch has completed M1 robustness Parts 1–6, the synthesis-only `stage126-m1-robustness-closure` action, and the `stage126-m1-retained-design-freeze` action (PR #65). The legacy closed-part/test/QC/Handoff mechanics are engineering debt discovered at the terminal M1 transition; they are not evidence that the M1 scientific results are invalid.

`stage127-m2-market-data-gate` is now HISTORICAL and CLOSED. Its human authorization already existed and the Gate was executed twice under that same authorization — first as UNRESOLVED with no data available, then re-executed and resolved from the imported authoritative TSETMC evidence bundle, returning a terminal, resolved `FAIL_M2_DATA_GATE`. M2 market data HAS been collected and independently validated — recorded separately from block admission — but it does not meet the frozen admission thresholds. That result stands unchanged. The human review it originally required has been completed: the human supervisor separately authorized `stage128-m2-boundary-month-return-design-freeze` (item 22) as the scientific action that follows the failed M2 extension.

The current scientific action is `stage128-m2-retained-block-human-decision` (item 24b), now COMPLETE: the human governance decision `RETAIN_M2_AS_INTERMEDIATE_CONFIRMATORY_BLOCK`, recorded exactly once under its own explicit one-action human authorization, built only by reading already-committed evidence and executing nothing. Its predecessor `stage127-m2-incremental-evaluation` (item 24) is complete: the paired, development-only comparison of the frozen M2 block against the frozen M1 block on the exact 539-row three-variable D2 common sample. The observed PR-AUC deltas are approximately null for all three frozen model families, with every 95% paired bootstrap interval including zero — that result is preserved and reported, not rewritten as support for M2. The preceding `stage128-m2-d2-gate-rerun` (item 23) is complete and is the data admission that evaluation consumed; its terminal result `PASS_FOR_M2_INCREMENTAL_EVALUATION` — **data admission only** — is preserved unchanged.

The current scientific action is `stage128-m3i2-prospective-contract-lock` (item 25a), a metadata-only prospective contract lock for the SUPPLEMENTARY M3I-2 block with a contingent, unresolved M3I-3 financing shell; it retrieved no data, executed no Gate and started no modeling, and it left the frozen CBI M3 contract (item 25) unchanged. The next research action is `stage128-m3i2-official-source-evidence-capture` (item 25b), a POINTER ONLY and **not** authorized. M2 is retained as the intermediate confirmatory block and as the comparator for a future paired `M3 − M2` evaluation, conditional on a separately authorized M3 data Gate that passes. Retention establishes no superiority: no winner has been selected, no final model has been selected, no full-development refit has occurred, M3/M4 remain unauthorized and unstarted, and the final test remains locked and untouched. The historical Stage127 D0 Gate (item 21) remains `FAIL_M2_DATA_GATE`, unchanged. The maintenance boundary may continue to be simplified under the lean-governance rules so long as scientific artifacts and the final-test firewall remain unchanged.

No M2/M3/M4 action, full-development refit, final-test access/evaluation, SHAP execution, winner/final-model selection, or new tuning is authorized merely by this roadmap update.

## Stage129 — M4 governance Data-Gate contract lock (DESIGN ONLY, additive item, not on either live pointer chain)

26. `stage129-m4-governance-data-gate-contract-lock` — **CONTRACT LOCK ONLY; DESIGN EXERCISE; zero retrieval, zero Gate execution, zero modeling, zero Final Test access.** A human supervisor authorized ONLY the design and prospective locking of an M4 Data Accessibility / Provenance Gate contract for the four-feature governance-predictor block, before any M4 value is retrieved. This action does not advance either live pointer chain above (`next_research_action_id: human-decision-required` and `m3_lag_wdi_next_action_id: human_decision_required` are both unchanged by this item); it publishes its own, separate, THIRD pointer instead (`stage129_m4_next_action_id: stage129-m4-governance-data-gate`, `stage129_m4_next_action_authorized: false`). **Corrected after independent supervisory audit** (candidate taxonomy_status/calendar_conversion_status fields added as explicit `CONTRACT_ISSUE_UNRESOLVED`, `join_identity_rule.required_identifier` frozen to the already-audited `ticker`/`fiscal_year_t` keys, and full Handoff-generator integration implemented via `derive_stage129_m4_governance_data_gate_contract_lock_markers` in `project/scripts/update_ai_handoff.py`): `handoff_state.json` and `CURRENT_STATE.md` are now regenerated by the canonical generator to expose the locked Stage129 state, not left untouched. **Candidate set, exactly four, no substitution:** `audit_opinion_type`, `going_concern_flag`, `audit_lag_days`, `board_size` — the same four identities already defined in the Stage125 Part 3b1 M4 feature-definition contract (`project/stage125/part3b1_m4_feature_definition_contract_stage125.json`, `decision_id: m4_feature_definitions`), inherited here verbatim and layered with a prospective Gate design: a forbidden-inference list for the going-concern flag, frozen source date fields/direction for the audit lag, a per-value provenance schema, a three-state Gate vocabulary (`PASS_M4_DATA_GATE` / `FAIL_M4_DATA_GATE` / `UNRESOLVED_M4_DATA_GATE`), join/identity rules and a default no-imputation missingness policy. Permanently excluded from M4: `non_executive_ratio`, `institutional_ownership`, any ownership/management feature, auditor-identity features, audit-fee features, textual/NLP/LLM-derived predictors, outcome-inspected keywords, and any governance feature "discovered later" — no failed candidate may be replaced to keep the count at four. **THE CONTRACT IS NOT COMPLETE — three unresolved prerequisites, all recorded as `CONTRACT_ISSUE_UNRESOLVED`:** (i) `audit_opinion_type` has no authoritative allowed-category taxonomy (only secondary Persian accounting-blog sources were found; no IACPA/Audit Organization standard text and no CODAL field schema), so no modeled categorical values are admitted and empirical category discovery is forbidden; (ii) `audit_lag_days` has no authoritative deterministic calendar-date conversion convention, and the M3-LAG-WDI `jalali_fiscal_year_t_plus_621` YEAR-MAPPING rule is explicitly forbidden as a daily date-conversion method, so no lag value may be computed; (iii) CROSS-CUTTING — no audited deterministic mapping resolves a CODAL issuer identity to the frozen parent-side `ticker` key (the stage127 join audit is parent-side only, its child side being TSETMC market data, and the Stage124 batch02 CODAL pilot was entirely network-blocked with zero evidence), so the Gate's join-quality dimension is not evaluable for ANY of the four candidates. Accordingly `m4_contract_complete=false`, `m4_contract_fully_executable=false`, `m4_data_gate_executable=false`, `m4_candidates_the_gate_may_execute_for=[]`, completion status `LOCKED_WITH_UNRESOLVED_PREREQUISITE_DEFINITIONS`. Candidate IDENTITY is frozen for all four; that is a different lock from the definitions and does not make the Gate executable. **Point-in-time rule inherited verbatim** from `project/stage125/part3b1_cutoff_available_at_contract_stage125.json` (`block_rules.M4: document_available_at_le_pair_cutoff`); a report's internal date is not its availability date. **Thresholds reused, not invented:** candidate development coverage ≥ 0.80, minimum training-fold coverage ≥ 0.75, and M4 block/common-sample coverage ≥ 0.70 are already canonical in `project/stage125/part4_statistical_analysis_plan_stage125.json`; the fourth threshold, ≥ 5 positive evaluable outcomes per locked validation fold, is already canonical in `project/stage128/m3_macro_data_gate/stage128_m3_macro_data_gate_decision.json` (field `min_positive_evaluable_each_temporal_validation_window`), not in the Stage125 Part 4 SAP. All four are reused unchanged by the M3 macro data Gate and the M3-LAG-WDI exploratory Data Gate; no conflict with any pre-existing frozen threshold was found. See `stage129_m4_data_gate_contract.json` → `thresholds.canonical_sources` for exact per-threshold provenance. **M3/comparator boundary unchanged:** M3-CBI stays `UNRESOLVED_M3_DATA_GATE`; M3-LAG-WDI stays `SUPPLEMENTARY_EXPLORATORY_ONLY` and is never promoted to confirmatory M3; the confirmatory Holm family (`M2_minus_M1`, `M3_CBI_minus_M2`, `M4_minus_M3_CBI`) is unchanged and unexecuted; M3-CBI's unresolved status is recorded as an explicit, unresolved future dependency for any M4-vs-M3-CBI incremental evaluation. **Final Test firewall untouched:** locked, `rows_read = 0`; neither this contract nor the future Gate it describes inspects an M4 value, target, distribution or coverage figure for a Final Test row. Contract-lock state: `M4 contract status = PROSPECTIVELY_LOCKED_PRE_RETRIEVAL`, `candidate count = 4`, `M4 data retrieval started = false`, `M4 candidate observations read = 0`, `M4 Data Gate executed = false`, `M4 block admitted = false`, `M4 modeling started = false`, `M4 incremental evaluation authorized = false`; authorization `was_authorized=true, authorized_now=false, authorization_consumed=true, authorization_reusable=false`; `next action pointer = stage129-m4-governance-data-gate`, `next action authorized = false` — a pointer, not an authorization. Package: `project/stage129/m4_governance_data_gate_contract/`; interpretation: `project/stage129/m4_governance_data_gate_contract/README_STAGE129_M4_GOVERNANCE_DATA_GATE_CONTRACT.md`; regression tests: `project/tests/test_stage129_m4_governance_data_gate_contract.py`.

27. `stage129-m4-authoritative-prerequisite-resolution` — **DOCUMENTARY RESEARCH ONLY; zero M4 retrieval, zero observation reads, zero coverage calculation, zero Gate execution, zero modeling, zero Final Test access.** A human supervisor authorized ONLY a bounded attempt to resolve, from authoritative documentary sources, the three prerequisites that item 26 records as `CONTRACT_ISSUE_UNRESOLVED` and that currently make the M4 Data Gate non-executable for every candidate. **Outcome: `DOCUMENTARY_RESEARCH_BLOCKED` — 0 of 3 prerequisites resolved.** All three verdicts are `BLOCKED_BY_ACCESS_OR_SOURCE_LIMITATION`, because each question terminates at an authoritative source that could not be reached from the execution environment: `codal.ir`, `audit.org.ir`, `tsetmc.com` and `seo.ir` all resolve into `198.18.0.0/15` (RFC 2544 range — sandbox DNS interception) and time out, while control hosts (`github.com`, `ifac.org`, `iacpa.ir`) return HTTP 200, so the failures are host-specific rather than a loss of network egress. **This is an access limitation of that environment and is explicitly NOT a finding that the documentation does not exist.** Substantive findings were nonetheless recorded: (i) for the opinion taxonomy, ISA 705 (Revised) was retrieved as primary text (SHA-256 `3517bbe5…`) and establishes exactly three modified-opinion types — qualified, adverse, disclaimer — which with ISA 700 (Revised)'s unmodified opinion gives a four-category structure, but it does **not** establish Iranian adoption, the existence of a CODAL *structured* opinion field, or CODAL's exact category identifiers, and the frozen Stage125 definition requires an explicit structured field rather than free-text inference, so the taxonomy stays unfrozen; IACPA (`iacpa.ir`) was reachable but publishes no taxonomy itself and links out to the unreachable `audit.org.ir`; (ii) for the calendar convention, CODAL date-field format documentation was unreachable, and two secondary findings cut against freezing a rule now — the official Solar Hijri calendar is **observational** (leap years follow the actual astronomical vernal equinox at the Iranian reference meridian, not a closed-form rule), and the repository-pinned `jdatetime==6.0.1` implements a fixed 33-year arithmetic approximation (`year % 33 in (1,5,9,13,17,22,26,30)`) whose only cited authority is Wikipedia; a 7,304-date round-trip check over 1385-01-01..1404-12-28 produced 0 mismatches but establishes only internal invertibility of that approximation, never agreement with the official calendar; (iii) for CODAL identity, no identifier field name could be confirmed, so no normalization, Unicode/Persian-Arabic character, ZWNJ, ticker-change, collision or one-to-one policy could be specified — and no fuzzy, name-based, manual or outcome-informed matching was substituted. **Nothing was resolved, so nothing is unlocked:** `m4_contract_complete=false`, `m4_contract_fully_executable=false`, `m4_data_gate_executable=false`, `m4_data_gate_authorized=false`, `m4_candidates_the_gate_may_execute_for=[]`, and all three issues remain in `contract_issues_unresolved`. Candidate identities, all four thresholds and the confirmatory Holm family are unchanged and the Holm family is unexecuted; M3-CBI stays `UNRESOLVED_M3_DATA_GATE`; M3-LAG-WDI stays `SUPPLEMENTARY_EXPLORATORY_ONLY`; Final Test stays locked with `rows_read = 0`; no paper winner, no final model, no full-development refit, no Stage130 work. The pointer `stage129_m4_next_action_id: stage129-m4-governance-data-gate` is unchanged and remains `authorized = false` — a pointer, never an authorization. Package: `project/stage129/m4_authoritative_prerequisite_resolution/`; interpretation: `project/stage129/m4_authoritative_prerequisite_resolution/README_STAGE129_M4_AUTHORITATIVE_PREREQUISITE_RESOLUTION.md`; regression tests: `project/tests/test_stage129_m4_authoritative_prerequisite_resolution.py`.

28. `stage129-m4-human-discontinuation-data-inadequacy` — **HUMAN GOVERNANCE DECISION ONLY; zero Gate execution, zero formal coverage computation, zero feature materialization, zero modeling, zero incremental evaluation, zero Holm, zero bootstrap, zero Final Test access.** A human supervisor decided, verbatim, *«موافقم M4 به علت پوشش ناکافی بسته شود»* ("I agree that M4 should be closed because of insufficient coverage"), and this item records ONLY that decision and its forward consequences. **Disposition: `M4_DISCONTINUED_BY_HUMAN_DECISION_DATA_INADEQUACY`.** **This is emphatically NOT a formal Gate verdict and is deliberately NOT recorded as `FAIL_M4_DATA_GATE`:** the M4 Data Gate was never executed and was never executable, no development-set or per-fold coverage threshold was ever computed, and no M4 feature was ever materialized (`m4_data_gate_executed=false`, `m4_formal_gate_verdict=null`, `observational_coverage_is_not_formal_gate_coverage=true`). The figures behind the decision are OBSERVATIONAL coverage over the whole 1331-row canonical population taken from the item-27-successor V4.3.1 custody package, not Gate output: 444/1331 rows with a verified auditor opinion, 446/1331 with an auditor report date, 889/1331 with a fiscal year end, 2214 field-level missing, 368 rows with only consolidated statements and 60 rows with no archive match. **Decisive for the definitions rather than the counts:** all 444 canonical opinions are FREE-TEXT derived while the frozen Stage125 definition of `audit_opinion_type` requires an explicit STRUCTURED field; the 65 payloads that do carry a genuine structured `نظر حسابرس` field are all fiscal years ۱۳۸۰–۱۳۹۰ and therefore contribute ZERO rows inside the canonical ۱۳۹۲–۱۴۰۲ window; `audit_lag_days` was never computed (calendar convention still `CONTRACT_ISSUE_UNRESOLVED`); `going_concern_flag` was never extracted; `board_size` was never prepared, so a complete four-candidate M4 block does not exist; and the CODAL-to-parent identity prerequisite is still unresolved. Large-scale manual completion is not pursued for this study given the size of the missingness. **Reason class is `data_accessibility_coverage_and_definition_mismatch` — NOT a poor model result and NOT outcome inspection**; the block was stopped BEFORE any M4 modeling and WITHOUT reading the Final Test (`reason_is_poor_model_result=false`, `reason_is_outcome_inspection=false`, `outcome_or_final_test_observation_used_for_this_decision=false`). **Forward state:** `m4_retrieval_continues=false`, `m4_manual_completion_continues=false`, `m4_feature_materialization_authorized=false`, `m4_modeling_will_run=false`, `m4_incremental_evaluation_will_run=false`, `m4_reopening_authorized=false`, `m4_reopening_requires_new_human_authorization=true` — reopening M4 for any purpose needs a NEW explicit human authorization that reverses this decision. **The four frozen candidates survive unchanged:** count stays exactly 4 and the set stays `audit_opinion_type`, `going_concern_flag`, `audit_lag_days`, `board_size`, with nothing removed, renamed or substituted — only the execution path for that block is stopped for this study, so the contract's own history stays readable. **Holm family unchanged and unexecuted:** `M4_minus_M3_CBI` status `NOT_EXECUTED_M4_DISCONTINUED`, p-value `null`, no null hypothesis accepted or rejected, family not modified and not shrunk post hoc — the frozen contract names family 2 `confirmatory_family_2_adjacent_block_gains_if_admitted`, so membership was PROSPECTIVELY conditional on admission and M4 was never admitted. How the manuscript should present a comparison belonging to a never-admitted block is NOT settled by the existing contract and is recorded as `manuscript_reporting_decision_for_the_unexecuted_m4_comparison: UNRESOLVED_REPORTING_DECISION` — nothing is invented. **History preserved, not rewritten:** the prospective contract lock (item 26), the 0-of-3 prerequisite outcome (item 27) and the observational V4.3.1 package all keep their markers and are untouched; the extraction stays `OBSERVATIONAL_TEXT_EXTRACTION_NOT_YET_ADMITTED_AS_LOCKED_M4_INPUT`, remains in repository custody, is NOT a model input, and MAY be reported in the limitations/data-access discussion. M1 and M2 results are unchanged; M3-CBI is NOT declared successful and stays `UNRESOLVED_M3_DATA_GATE`; M3-LAG-WDI stays `SUPPLEMENTARY_EXPLORATORY_ONLY`; no paper winner, no final model, no full-development refit, no Stage130 work; Final Test stays locked with `rows_read = 0`. The M4 pointer no longer names an M4 execution step: `stage129_m4_next_action_id` moves from `stage129-m4-governance-data-gate` to `human_decision_required`, scope `m4_discontinued_no_further_m4_action_is_authorized`, `authorized = false` — a pointer, never an authorization. Package: `project/stage129/m4_human_discontinuation_data_inadequacy/`; interpretation: `project/stage129/m4_human_discontinuation_data_inadequacy/README_STAGE129_M4_HUMAN_DISCONTINUATION_DATA_INADEQUACY.md`; regression tests: `project/tests/test_stage129_m4_human_discontinuation_data_inadequacy.py`.

29. `stage129-m4-manuscript-reporting-decision` — **HUMAN REPORTING/GOVERNANCE DECISION ONLY; zero extraction rerun, zero payload re-inspection, zero download or data completion, zero Gate execution, zero formal or per-fold coverage computation, zero feature materialization, zero modeling, zero incremental evaluation, zero Holm, zero bootstrap, zero SHAP, zero prediction, zero Final Test access, zero Stage130.** Item 28 recorded truthfully that the frozen Stage125 contract makes family-2 membership PROSPECTIVELY conditional on block admission but says nothing about how to PRESENT the comparison of a block that was never admitted, and therefore left `manuscript_reporting_decision_for_the_unexecuted_m4_comparison: UNRESOLVED_REPORTING_DECISION`. A human supervisor has now decided that presentation question, and this item records ONLY that decision. **Canonical value: `REPORT_AS_PRESPECIFIED_NOT_EXECUTED_DATA_INADEQUACY_NO_INFERENCE`.** The decided position, every part of which is a statement about what did NOT happen: M4 was a PRESPECIFIED block; it was stopped BEFORE admission and BEFORE any modeling because coverage was inadequate and the frozen feature definitions were not satisfied; `M4_minus_M3_CBI` stays `NOT_EXECUTED_M4_DISCONTINUED` with p-value `null`; **no null hypothesis is accepted or rejected**; **no inferential conclusion and no performance claim is made about M4**; and the comparison is NOT deleted from, renamed in or substituted out of the SAP history — it is reported as prespecified-but-not-executed. **Still emphatically NOT `FAIL_M4_DATA_GATE`:** the Gate was never executed (`m4_data_gate_executed=false`) and the formal verdict stays `null`; this item settles presentation, not evaluation. **Approved reporting text (EN), recorded verbatim in the decision artifact:** *“M4 was prespecified but was not admitted to modeling because the available data did not provide adequate coverage and did not satisfy the frozen feature definitions. Consequently, the M4−M3-CBI comparison was not executed, no p-value was computed, and no inferential conclusion is drawn for M4.”* An equivalent Persian text is recorded alongside it. This is the APPROVED REPORTING TEXT ONLY (`APPROVED_REPORTING_TEXT_ONLY_NOT_A_MANUSCRIPT_WRITING_AUTHORIZATION`) and is NOT an authorization to write or rewrite the manuscript (`manuscript_writing_or_rewriting_authorized=false`). **History superseded in the open, never rewritten:** the item-28 package stays byte-for-byte intact and its boundary artifact still reads `UNRESOLVED_REPORTING_DECISION`, because that is what was true at the moment of the discontinuation; this item supersedes THAT SINGLE MARKER and nothing else, declaring the superseded file, key, previous value and resolved value machine-readably on both sides, and the canonical generator publishes the resolved value to the Handoff. **Scientific state unchanged:** disposition stays `M4_DISCONTINUED_BY_HUMAN_DECISION_DATA_INADEQUACY`; the four frozen candidates keep their identity, order and count (`audit_opinion_type`, `going_concern_flag`, `audit_lag_days`, `board_size`); the Holm family is unmodified and unexecuted; M1 and M2 results are unchanged; M3-CBI stays `UNRESOLVED_M3_DATA_GATE`; M3-LAG-WDI stays `SUPPLEMENTARY_EXPLORATORY_ONLY`; the observational V4.3.1 extraction keeps its custody status and is still not a model input; no paper winner, no final model, no full-development refit, no Stage130; Final Test stays locked with `rows_read = 0`. The M4 pointer is NOT advanced and neither live research pointer chain moves: `m4_next_action_id` stays `human_decision_required`, scope `m4_discontinued_no_further_m4_action_is_authorized`, `authorized = false` — a pointer, never an authorization. Package: `project/stage129/m4_manuscript_reporting_decision/`; interpretation: `project/stage129/m4_manuscript_reporting_decision/README_STAGE129_M4_MANUSCRIPT_REPORTING_DECISION.md`; regression tests: `project/tests/test_stage129_m4_manuscript_reporting_decision.py`.

30. `stage129-m3-cbi-human-discontinuation-and-reporting` — **HUMAN SCIENTIFIC DECISION PLUS ITS REPORTING CONSEQUENCE; zero retrieval, zero download, zero new contact or follow-up with the World Bank or any official source, zero Gate re-execution, zero new coverage or threshold computation, zero feature materialization, zero modeling, zero incremental evaluation, zero Holm, zero bootstrap, zero SHAP, zero prediction, zero Final Test access, zero Stage130, zero change to any existing PR.** A human supervisor decided, verbatim, *«موافقم M3-CBI به علت حل‌نشدن Data Gate و نبود شواهد قابل‌اثبات point-in-time، بدون ورود به مدل‌سازی متوقف شود»*, and this item records ONLY that decision and how the block's never-executed confirmatory comparison is reported. **Disposition: `M3_CBI_DISCONTINUED_BY_HUMAN_DECISION_UNRESOLVED_DATA_GATE_AND_UNPROVEN_POINT_IN_TIME`.** **The methodological point that separates this from item 28:** M4 never had a Gate run at all, but M3-CBI's Gate WAS executed — `stage128-m3-macro-data-gate` ran, its authorization was consumed, and it produced a terminal snapshot. So this item is careful in BOTH directions and refuses both falsifications: it is **NOT** recorded as `FAIL_M3_DATA_GATE` (that verdict is in the Gate's locked vocabulary and the Gate did not return it — it terminated unresolved because the prospective definition lock stayed incomplete and no independently verifiable official evidence existed, not because a candidate was assessed against a threshold and failed; no value-level assessment was ever executed), and it is **NOT** recorded as an unexecuted Gate either, because erasing a completed research action would be its own falsification. The Gate's own status is republished, never reassigned: `m3_macro_data_gate_executed = true`, `m3_macro_data_gate_terminal_status = UNRESOLVED_M3_DATA_GATE`, `m3_macro_data_gate_formal_failure = false`. **Nothing is fabricated as resolved:** all **8** recorded unresolved/blocker reasons stand (`m3_macro_data_gate_unresolved_reasons_resolved = 0`), no new coverage or threshold was computed, and `point_in_time_availability_established = false` — the definition lock was incomplete for all three candidates (14 of 20 required lock fields unresolved each), no official CBI data or documentation artifact is committed, the access-probe capture metadata is not independently verifiable, and G04 (*published_at or available_at verified*) was never satisfied. **Reason class is `unresolved_executed_data_gate_and_unproven_point_in_time_availability` — NOT a poor model result and NOT outcome inspection**; the block was stopped BEFORE admission and BEFORE any modeling and WITHOUT reading the Final Test. **The prespecified comparison is kept, not erased:** live canonical id `M3_CBI_minus_M2` (already published by the merged M4 packages — no new alias is minted) with the frozen Stage125 contract's own member name `M3_minus_M2` recorded alongside it and left untouched; status `NOT_EXECUTED_M3_CBI_DISCONTINUED`, p-value `null`, null hypothesis accepted `false` and rejected `false`, inferential conclusion `none`, performance claim `none`, not removed from SAP history, not renamed, not substituted, family not shrunk. Holm final adjustment is NOT declared complete and unexecuted comparisons keep their `null` p-values. **M3-LAG-WDI is NOT promoted into the role M3-CBI vacated:** it stays `SUPPLEMENTARY_EXPLORATORY_ONLY` with role `supplementary_exploratory_robustness_block`, is neither substitute nor proxy nor representative nor a confirmatory M3, its results are not used to fill the `M3_CBI−M2` comparison, no exploratory p-value or result enters the confirmatory Holm family, and its existing results and artifacts are unchanged. PR #79 is read READ-ONLY as an operational/historical fact (state `MERGED`, 2026-08-08); this decision authorizes no change to it or to any other existing PR. **Approved reporting text (EN), recorded verbatim in the decision artifact:** *“M3-CBI was prespecified, but its executed Data Gate remained unresolved because point-in-time availability could not be established. The block was therefore not admitted to modeling. Consequently, the M3-CBI−M2 comparison was not executed, no p-value was computed, and no inferential conclusion is drawn for M3-CBI. The M3-LAG-WDI analysis is reported separately as supplementary exploratory evidence and is neither a substitute nor a proxy for confirmatory M3-CBI.”* An equivalent Persian text is recorded alongside it; both are APPROVED REPORTING TEXT ONLY and are NOT an authorization to write or rewrite the manuscript (`m3_cbi_manuscript_writing_authorized = false`). **History superseded in the open, never rewritten:** the executed Gate handed its disposition question to a human (`m3_macro_data_gate_human_review_required = true`) and no human disposition had been recorded; this item answers THAT PENDING REVIEW and nothing else, naming the artifact, key, previous value and resolved value machine-readably, while the Gate package stays byte-for-byte intact and its `gate_status` is not reassigned. **Unchanged elsewhere:** M1 and its results; M2 and the decision to retain it; the M4 discontinuation and reporting decisions merged in PR #84 and PR #85; the M3I-2 artifacts. No paper winner, no final model, no full-development refit, no Stage130; Final Test stays locked with `rows_read = 0`; `next_research_action_authorized` stays `false`. The M3-CBI pointer is `human_decision_required`, scope `m3_cbi_discontinued_no_further_m3_cbi_action_is_authorized`, `authorized = false` — a pointer, never an authorization. Package: `project/stage129/m3_cbi_human_discontinuation_and_reporting/`; interpretation: `project/stage129/m3_cbi_human_discontinuation_and_reporting/README_STAGE129_M3_CBI_HUMAN_DISCONTINUATION_AND_REPORTING.md`; regression tests: `project/tests/test_stage129_m3_cbi_human_discontinuation_and_reporting.py`.

31. `stage129-final-development-model-eligibility-audit` — **READ-ONLY AUDIT OVER COMMITTED ARTIFACTS; zero fits, zero predictions, zero `predict_proba`/`decision_function`, zero tuning, zero feature or threshold search, zero bootstrap or resampling, zero new metric/CI/p-value, zero calibration, zero SHAP, zero row-level scientific data reads, zero historical artifact edits, zero winner or final-model selection, zero refit, zero Stage130, zero Final Test access.** The audit answers one question from the frozen record alone: do the already-locked rules and already-committed results determine the final predictor block and the final algorithm uniquely, or is a separate human decision still required? **Verdicts: `FINAL_BLOCK_REQUIRES_HUMAN_DECISION` and `FINAL_ALGORITHM_REQUIRES_HUMAN_DECISION`, with `audit_determined_candidate = null` for both.** Both rest on an ABSENCE of a rule rather than a conflict between rules — hence neither is a `CONTRACT_CONFLICT_...` verdict — and six candidates are eligible, so neither is a `NO_*_CURRENTLY_ELIGIBLE...` verdict either. **Eligible blocks are M1 and M2 only**; M3-CBI (item 30) and M4 (item 28) were both discontinued before admission and before any modeling, so neither ever produced a model to be eligible. **`M2 retained` is not `M2 selected`:** the merged retention decision lists `final_model_selection` and `paper_winner_selection` under `m2_retention_does_not_imply`, gives its basis as `preregistered_nested_confirmatory_architecture_preservation_not_observed_predictive_superiority`, sets `decision_is_a_retained_block_decision_not_a_superiority_decision = true` and assigns `m2_role = intermediate_confirmatory_block` — a structural role, not a selection. **The absence of M2 superiority does not force M1:** no frozen artifact states which block is final when superiority is absent, so concluding "M2 failed, therefore M1" would be inference rather than rule-following; the ambiguity is recorded and nothing is selected. On the 366-row paired common sample **no PR-AUC confidence interval excludes zero** for any family and the point-estimate signs disagree (`families_agree_on_point_estimate_sign = false`); Brier intervals do exclude zero for all three but in CONFLICTING directions (random forest worse, logistic and xgboost better), so a secondary metric that disagrees with the primary metric cannot break a tie no frozen rule authorizes breaking. **No unique algorithm follows**, for four independent reasons: (1) no model-selection rule exists anywhere — the metrics/uncertainty contract fixes `primary_metric = PR-AUC` for REPORTING, its only winner clause is the prohibition `do_not_select_winner_on_calibrated_final_test`, and its only `tie_break` chooses a THRESHOLD, not a model; (2) the retained design freeze retains ALL THREE families with `final_model_selected = false`, so a selected configuration per algorithm is not a selected final algorithm; (3) the ordering is unstable; (4) `confirmatory_family_1` — the sole prespecified inferential ranking mechanism — was never executed. The robustness closure states outright that the evidence *“does NOT justify … selecting a winning model family”* and lists `winner_selection` among its prohibited actions. **Robustness ordering reported, never used to cherry-pick:** the locked primary pooled ordering is logistic (0.4458) > random forest (0.4024) > xgboost (0.3565); Parts 2, 3, 4, 5 and 6 preserve it, **Part 1 fully reverses it** (xgboost > random forest > logistic), and Part 5 preserves the pooled ordering while **inverting the top two families within fold 1** (RF 0.6010 > logistic 0.5664). No candidate is excluded on these grounds and no robustness part is promoted to primary. **Holm family reconciled, not executed:** all three members survive unrenamed — `M2_minus_M1` `EVALUATED_NO_SUPERIORITY_ESTABLISHED`, `M3_CBI_minus_M2` `NOT_EXECUTED_M3_CBI_DISCONTINUED`, `M4_minus_M3_CBI` `NOT_EXECUTED_M4_DISCONTINUED`, every p-value `null`, family not shrunk post hoc, `holm_family_complete` still `false`, zero new p-values created (no p-value for `M2_minus_M1` exists at all — the committed evidence is bootstrap confidence intervals). The frozen multiplicity contract is silent on closing a family two of whose three prespecified members were never executed, so the reporting verdict is `HOLM_FINAL_REPORTING_REQUIRES_SEPARATE_HUMAN_OR_METHODS_DECISION`. **Surfaces are not conflated:** M1 pooled-OOF figures come from the 1263-row primary development lock while M2 figures come from the 366-row paired common sample (whose own M1 arm reads logistic 0.477497, RF 0.355559, xgboost 0.355762); the audit does not compare across them. **Nothing is opened:** `paper_winner_selected = false`, `final_model_selected = false`, `full_development_refit_performed = false`, `stage130_started = false`, `final_test_access_authorized = false`, Final Test locked with `rows_read = 0`, `next_research_action_authorized = false`. M1, M2, M3-CBI, M4 and M3-LAG-WDI all keep their existing dispositions. The audit pointer is `human_decision_required` — an audit verdict is never an authorization. Package: `project/stage129/final_development_model_eligibility_audit/`; interpretation: `project/stage129/final_development_model_eligibility_audit/README_STAGE129_FINAL_DEVELOPMENT_MODEL_ELIGIBILITY_AUDIT.md`; regression tests: `project/tests/test_stage129_final_development_model_eligibility_audit.py`.

32. `stage129-final-model-human-selection-governance` — **HUMAN GOVERNANCE SELECTION DECISION ONLY; zero fits, zero predictions, zero full-development refit, zero trained-artifact creation, zero tuning, zero calibration, zero threshold search, zero bootstrap, zero SHAP, zero new metric/CI/p-value, zero Holm execution, zero row-level scientific data reads, zero Final Test access, zero Stage130.** Item 31's read-only audit found no frozen rule determining either the block or the algorithm and returned `FINAL_BLOCK_REQUIRES_HUMAN_DECISION` / `FINAL_ALGORITHM_REQUIRES_HUMAN_DECISION`. A human supervisor has now made the decision that audit called for. **Final development block `M1`; final algorithm `regularized_logistic_regression`; configuration `logistic__C_0.1`.** **`paper_winner_selected = true`.** **The basis is `HUMAN_DECISION_BASED_ON_PRELOCKED_DEVELOPMENT_EVIDENCE`** — this decision does NOT retroactively create the frozen rule the audit looked for, which is precisely why the basis is recorded as a human decision rather than a rule-derived determination. **It is emphatically NOT an inferential result:** `inferential_superiority_claimed = false`, `is_tested_superiority = false`, `is_holm_result = false`, `is_statistical_proof = false`, `selection_used_holm_result = false`. No hypothesis test selected this model — `confirmatory_family_1` was never executed, so no adjusted inferential ranking of the three algorithms exists, and the development evidence behind the decision was locked beforehand and is quoted, never recomputed. **M2 keeps its role** `intermediate_confirmatory_block`; not selecting M2 is NOT a statistical failure of M2 and is not reported as one (`m2_declared_statistically_failed = false`, `m2_not_selected_is_not_a_statistical_failure = true`), and `m2_predictive_superiority_claim_supported` stays `false` — an absence of demonstrated superiority, not a demonstrated absence. **Random forest and XGBoost are not removed, rejected or declared statistically inferior:** both keep their frozen configurations with status `NOT_SELECTED_BY_HUMAN_DECISION_ONLY` and all three of `declared_removed` / `declared_rejected` / `declared_statistically_inferior` set `false`. **Holm family preserved, never executed — status `HOLM_NOT_EXECUTED_FAMILY_PRESERVED_NO_INFERENCE`:** all three members survive with `M2_minus_M1 = EVALUATED_NO_SUPERIORITY_ESTABLISHED`, `M3_CBI_minus_M2 = NOT_EXECUTED_M3_CBI_DISCONTINUED`, `M4_minus_M3_CBI = NOT_EXECUTED_M4_DISCONTINUED`, every p-value `null`, no hypothesis accepted or rejected, family neither shrunk nor redefined, `holm_family_complete` still `false`, zero new p-values, and the frozen SAP counterparts untouched. **The distinction this item exists to keep:** the block and algorithm family ARE selected; the development configuration was already locked before the decision (`final_configuration_selected_by_this_decision = false`); but **no full-development model was refit** (`full_development_refit_performed = false`), **no trained final-model artifact was created** (`trained_final_model_artifact_created = false`, `trained_model_artifacts_written = 0`), and **the Final Test is still locked and unread** (`final_test_locked = true`, `final_test_rows_read = 0`, `final_test_access_authorized = false`, `stage130_started = false`). Naming which model the paper reports is a governance act; producing the fitted object and evaluating it are separate, still-unauthorized steps. **History superseded in the open:** this item supersedes the audit's two 'requires human decision' verdicts and its `human_decision_required` pointer AND NOTHING ELSE; the audit package stays byte-for-byte intact and its findings remain the historical record, with the superseded artifact, keys, previous values and their meaning recorded machine-readably. The next pointer is `human_authorization_required_for_full_development_refit_and_final_test` with `authorized = false` — a pointer, never an authorization. Package: `project/stage129/final_model_human_selection_governance/`; interpretation: `project/stage129/final_model_human_selection_governance/README_STAGE129_FINAL_MODEL_HUMAN_SELECTION_GOVERNANCE.md`; regression tests: `project/tests/test_stage129_final_model_human_selection_governance.py`.

33. `stage129-full-development-refit-contract-lock` — **CONTRACT AUDIT AND LOCK ONLY; zero fits, zero predictions, zero `predict_proba`/`decision_function`, zero tuning or retuning, zero model or configuration re-selection, zero feature or threshold search, zero bootstrap, zero calibration, zero SHAP, zero new metric/CI/p-value, zero row-level scientific data reads for analysis, zero Final Test access, zero Stage130.** Records what a full-development refit of the selected `M1 / regularized_logistic_regression / logistic__C_0.1` model would be permitted to do, **if it were ever authorized**. Status: `PROSPECTIVELY_LOCKED_NOT_EXECUTED`. **Every term is EXTRACTED from an already-frozen artifact and each carries its source file and key** (`no_term_invented_by_this_action = true`); all **12** source artifacts are pinned by SHA-256 and the generator re-hashes them, so a drifted source fails the build. **Authorized data:** sample `main_rule_a_primary`, target `FD_target_main_t_plus_1`, `analysis_ready_main_rule_a_stage125.csv` and its audited-pairs companion pinned to the frozen SAP's own `pinned_part3c_input_sha256`, **fit target years ۱۳۹۳–۱۳۹۹** taken from the frozen preprocessing contract's own `final_development_refit.fit_years`, split variable `target_year`, random split and shuffle both unauthorized. **The one substantive thing this audit had to resolve:** the frozen contract sets `fit_scope = each_temporal_training_fold_separately`, which is a DEVELOPMENT statement describing the two-fold cross-validation; a full-development refit has no folds, so the contract records the fit scope explicitly as the single ۱۳۹۳–۱۳۹۹ window with every training-derived statistic — clipping bounds, medians, standardization mean/std — re-estimated on that one fit set and **no statistic carried over from any fold**. That is not a new rule; it is the same frozen `final_development_refit` block applied consistently, called out because reusing fold-level statistics would be the easiest way to silently break the pipeline. **Features:** the 9-feature `M1_PRIMARY_FEATURE_ORDER` in exact locked order, with `revenue_growth_period_adjusted` still excluded on its frozen coverage-gate rejection and all four feature-selection methods forbidden. **Preprocessing and missing handling:** the eight-step continuous pipeline verbatim — capture the original pre-imputation mask first, estimate 1st/99th clipping bounds on observed fit-set values BEFORE median imputation, impute on the clipped fit-set median, append missingness indicators unstandardized binary 0/1, and standardize imputed continuous features (logistic only) on fit-set mean/std; target states exact with **missing never counted as negative**. **Threshold:** `development_OOF_F2_maximizing_threshold`, tie-break `higher_threshold` — recorded explicitly as a DEVELOPMENT-OOF quantity that is neither re-derived from the refit model's own in-sample predictions nor ever optimized on the Final Test. **Seeds:** the frozen budget records `logistic_regression_deterministic = true` and scopes the five-seed averaging rule to random forest and XGBoost, so **the selected logistic configuration needs no fit seed and no seed averaging**; the locked seeds and the bootstrap seed `20260724` are reference-only and introducing any new seed is unauthorized. **Environment:** must match the locked development runtime exactly (python 3.13.5, scikit-learn 1.9.0, numpy 2.4.6, pandas 3.0.3, jdatetime 6.0.1, xgboost 3.3.0), else `FAIL_CLOSED_DO_NOT_REFIT`. **Calibration:** primary probabilities stay raw and uncalibrated, isotonic unauthorized, Platt optional and only ever fit on pooled development OOF; the frozen `skip_recalibration_if_oof_positives_lt: 20` rule does NOT trigger because the selected model has **35** pooled development-OOF positives (quoted from the committed metrics artifact), and executing recalibration is still unauthorized. **Expected outputs:** four artifacts — fitted model, fit-set preprocessing parameters, provenance record and QC report — every one `exists_now: false`; forbidden outputs include any Final Test prediction or metric, any new hyperparameter-search result, any recalibrated primary probability, and any new development metric replacing the locked primary results, which this refit explicitly does not replace. **Twelve fail-closed controls FC01–FC12, each `ABORT_REFIT`:** input hashes, runtime match, **zero rows with `target_year` in ۱۴۰۰–۱۴۰۲ in the fit set**, exact feature order and count, exact hyperparameters, mask-before-imputation, clipping-before-imputation, no search/expansion/early-stopping, `final_test_rows_loaded == 0`, locked primary results byte-identical before and after, threshold read from the development-OOF rule, and **exactly one model fitted**. **Final Test boundary:** years ۱۴۰۰–۱۴۰۲ stay locked, unread and unauthorized (`final_test_rows_read = 0`); the refit may not read, predict on or evaluate against them; the frozen `apply_once_to_locked_final_test` clause is acknowledged as a FUTURE separately authorized step and recording it is not permission to take it; the expected final-test counts (346/12/334) are quoted as frozen metadata from the temporal split contract with no row-level access. **Locking authorizes nothing:** `refit_execution_authorized = false`, `refit_execution_requires_new_explicit_human_authorization = true`, `full_development_refit_performed = false`, `trained_final_model_artifact_created = false`, `stage130_started = false`, and every counter including `full_development_refits_executed` is `0`. The pointer is `human_authorization_required_for_full_development_refit_execution` with `authorized = false` — a locked contract is never an execution permission. Package: `project/stage129/full_development_refit_contract_lock/`; interpretation: `project/stage129/full_development_refit_contract_lock/README_STAGE129_FULL_DEVELOPMENT_REFIT_CONTRACT_LOCK.md`; regression tests: `project/tests/test_stage129_full_development_refit_contract_lock.py`.

34. `stage129-full-development-refit-execution` — **THE ONE-TIME CONTRACTED REFIT, EXECUTED. Exactly ONE model fitted, on the Development window only. Zero tuning, zero retuning, zero model re-selection, zero feature search, zero threshold search, zero bootstrap, zero recalibration, zero SHAP, zero predictions, zero new scientific metric/CI/p-value, zero Stage130, zero Final Test access.** Executes exactly what the contract locked and merged in PR #89 permits. **Model:** `M1 / regularized_logistic_regression / logistic__C_0.1` with `C=0.1`, `penalty=l2`, `solver=liblinear`, `max_iter=5000`, `class_weight=balanced`. **Fit set:** development target years **۱۳۹۳–۱۳۹۹**, **۶۶۶** rows (۶۸ positive / ۵۹۸ negative) across 110 tickers — the deduplicated union of the four development fold-roles, NOT the 1012-row full sample (which contains the locked final-test partition) and NOT the 421-row pooled OOF surface. **Design matrix:** 18 columns — 9 continuous features in locked `M1_PRIMARY_FEATURE_ORDER` plus 9 unstandardized binary missingness indicators; converged at `n_iter = 6`. **The pipeline was reused, not reimplemented:** the executor imports `project/src/stage126_m1_primary_development_tuning.py` — the same module that produced the locked primary results — and calls its own `build_development_allowlist`, `load_development_values`, `fit_preprocessor` and `transform`; only the fit set differs (one window instead of two folds). That also means the Final Test firewall is a property of the CODE PATH: the loader streams the analysis-ready CSV and, for any key on the final-test denylist, skips the row without parsing a single value. **Preprocessing re-estimated on this fit set only:** clipping bounds (1st/99th), medians of the clipped observed values, and standardization mean/std were all estimated on the single ۱۳۹۳–۱۳۹۹ fit set, with **nothing carried over from a development fold** — the contract's central requirement. FC07 PROVES the ordering rather than asserting it: it recomputes the bounds and the median-of-clipped-observed directly from the raw fit set and compares, so an impute-before-clip pipeline would have aborted the run. **All twelve fail-closed controls FC01–FC12 PASS**, each of which aborts on failure with no continue-on-error path: input SHA-256s (3 pinned inputs), runtime match (python 3.13.5, scikit-learn 1.9.0, numpy 2.4.6, pandas 3.0.3, jdatetime 6.0.1, xgboost 3.3.0), fit window with **zero** Final Test years, exact feature order and count with the prohibited growth feature absent, exact hyperparameters, unstandardized binary missingness indicators, clipping-before-imputation, no search/expansion/early-stopping, `final_test_rows_loaded == 0`, the four locked primary artifacts byte-identical before and after, threshold read from the development-OOF rule rather than re-derived, and **exactly one model fitted**. **Four contracted outputs produced and hashed:** the fitted model (serialized as explicit intercept + 18 coefficients, **not a pickle**, so it is exactly reproducible, diffable and auditable and keeps a binary blob out of the repository), the fit-set preprocessing parameters, the provenance record (contract hash, input hashes, runtime, fit-set definition, pipeline-module hash) and the QC report. **Determinism** was confirmed at zero extra cost — the dry run and the writing run produced byte-identical artifact hashes — and no second model was fitted to test it, because FC12 permits exactly one fit; `liblinear` + L2 is deterministic and the call reuses the already-locked seed `20260719` so **no new seed was introduced**. **Nothing scientific was produced:** no prediction was generated, no metric, CI or p-value computed, the Holm family is untouched at `HOLM_NOT_EXECUTED_FAMILY_PRESERVED_NO_INFERENCE`, and the locked primary development results are byte-identical and are **not** replaced by this refit. **Final Test firewall held:** `final_test_locked = true`, `final_test_rows_read = 0`, `final_test_access_authorized = false`, `stage130_started = false`; no final-test row, predictor, target, prediction or metric was read or produced. The pointer is `human_authorization_required_for_final_test_access` with `authorized = false` — applying this model to the Final Test is a separate step needing new explicit human authorization. Package: `project/stage129/full_development_refit_execution/`; executor: `project/src/stage129_full_development_refit.py`; interpretation: `project/stage129/full_development_refit_execution/README_STAGE129_FULL_DEVELOPMENT_REFIT_EXECUTION.md`; regression tests: `project/tests/test_stage129_full_development_refit_execution.py`.

35. `stage129-final-test-execution-contract-lock` — **CONTRACT AUDIT AND LOCK ONLY; READ-ONLY OVER COMMITTED ARTIFACTS. Zero Final Test rows, predictors, targets or predictions read for target years ۱۴۰۰–۱۴۰۲, zero fits, zero refits, zero predictions, zero tuning, zero model re-selection, zero feature or threshold search, zero bootstrap, zero recalibration, zero SHAP, zero Holm execution, zero new metric/CI/p-value, zero threshold materialization, zero Stage130.** Records what a future, separately authorized one-time Final Test execution of the selected `M1 / regularized_logistic_regression / logistic__C_0.1` model would be permitted to do, **if it were ever authorized**. Status: `PROSPECTIVELY_LOCKED_NOT_EXECUTED`. **Every term is EXTRACTED from an already-frozen artifact and each carries its source file and key** (`no_term_invented_by_this_action = true`); all **17** source artifacts are pinned by SHA-256 and the generator re-hashes them, so a drifted source fails the build. **What the contract accepts — and nothing else:** the selected model, plus exactly the **four** Full-Development Refit artifacts merged in PR #90, each pinned by SHA-256 and re-hashed by `FT01`; random forest and xgboost are not accepted as input, which is a scope statement about this contract and **not** a claim that either is statistically inferior — both keep `NOT_SELECTED_BY_HUMAN_DECISION_ONLY`. **The model is APPLIED, never refitted:** `model_fits_executed = 0`, the fitted object is reconstructed from the pinned intercept and 18 coefficients, and the frozen eight-step pipeline appears as **six** steps because the three ESTIMATION steps are already fixed by the refit — none is added and none is reordered. The one asymmetry is drawn verbatim from the frozen preprocessing contract: the Final Test missingness mask comes from the Final Test rows' **own** original pre-imputation positions while the imputation VALUE comes from the refit fit set, because reading which of a row's own fields are blank is not estimating a statistic. **The one substantive thing this audit had to resolve:** the frozen metrics contract fixes the operating-threshold RULE (`development_OOF_F2_maximizing_threshold`) and its TIE-BREAK (`higher_threshold`), but **no committed artifact anywhere in the repository contains the resulting number — it was never computed**. Two frozen artifacts name the rule (the metrics contract and the retained design freeze) and both carry the rule and tie-break and nothing numeric. This action does **not** compute it, because that would be a new derived quantity; instead the gap is recorded as unsatisfied prerequisite `PRE02`, the derivation inputs are pinned (the 1263-row pooled development-OOF surface, **zero** Final Test rows required), and `FT10` blocks every thresholded output until a committed value exists. `Recall@10%` and `Lift@10%` are **top-K** metrics under `K_y = ceil(0.10 * N_y)` and do **not** depend on it — but that is **arithmetic, not a permission**. There is NO threshold-free escape hatch: `PRE02` has exactly one resolution (the value is computed and committed under its own human authorization), deriving it is a separate Development-only action, and `FT21` aborts any partial, threshold-free or metric-subset run. **The Final Test is opened once, after every prerequisite is resolved, never in stages.** While `PRE01` and `PRE02` stand unresolved the contract publishes `final_test_contract_fully_executable = false`, `final_test_execution_authorized = false`, `final_test_access_authorized = false` and `final_test_rows_read = 0` directly, rather than leaving executability to be inferred. **Metrics locked before access:** primary `PR-AUC`; secondary `ROC-AUC`, `Brier_score`, `Recall@10%`, `Lift@10%`; the set is CLOSED, substitution and addition are unauthorized, and the primary metric may not be changed after seeing results. **Uncertainty locked, unexecuted:** `paired_company_cluster_bootstrap` clustered on `ticker`, 2000 replicates, minimum 1000 valid, percentile-95, seed `20260724` — with the caveat recorded, and no frozen parameter changed, that the expected Final Test positive count is **12**. **Calibration:** primary probabilities stay raw, isotonic unauthorized, Platt only ever on pooled development OOF and unexecuted; the frozen `skip_recalibration_if_oof_positives_lt: 20` rule does not trigger at **35** pooled OOF positives. **No inference:** `confirmatory_family_1` was never executed, evaluating one model creates no p-value, closes no Holm family and establishes no superiority; the family may not be shrunk or redefined post hoc and Final Test results may never reopen model selection. **Twenty-one fail-closed controls FT01–FT21, each `ABORT_FINAL_TEST`.** Note `FT05` is the MIRROR IMAGE of the refit's `FC03`: there Final Test years were forbidden in the fit window, here development years are forbidden in the evaluation window, because evaluating on development rows would silently report an in-sample result. **Prerequisites recorded honestly — two are UNSATISFIED:** `PRE01` a new explicit human authorization for Final Test access, and `PRE02` the missing threshold value, which has no threshold-free alternative. **Locking authorizes nothing:** `final_test_execution_authorized = false`, `final_test_access_authorized = false`, `final_test_rows_read = 0`, recalibration / refit / second-fit / tuning / new-scientific-result all `false`, `stage130_started = false`, `merge_authorized = false`, `ready_for_review_authorized = false`, and every governance counter is `0`. The pointer is `human_authorization_required_for_final_test_execution` with `authorized = false` — a locked contract is never an execution permission. Package: `project/stage129/final_test_execution_contract_lock/`; interpretation: `project/stage129/final_test_execution_contract_lock/README_STAGE129_FINAL_TEST_EXECUTION_CONTRACT_LOCK.md`; regression tests: `project/tests/test_stage129_final_test_execution_contract_lock.py`.

36. `stage129-threshold-derivation-algorithm-contract-lock` — **ALGORITHM CONTRACT AUDIT AND LOCK ONLY; READ-ONLY OVER COMMITTED METADATA. Zero threshold derived, zero probability values read, zero model fits, zero predict_proba calls, zero recalibration, zero tuning, zero bootstrap, zero SHAP, zero p-values, zero new scientific result, zero Final Test access, zero Stage130.** Locks **how** the development-OOF F2-maximizing threshold would be computed, and computes nothing. `threshold_value = null`. Only the pinned input's SHA-256, byte count and header row were read — **no `predicted_probability` or `observed_target` value**. **This contract is deliberately NOT a pure extraction, and says so.** Items 33 and 35 could declare `every_term_is_extracted_from_a_prelocked_artifact = true`; this one declares it **false**. The item 35 audit found the frozen record fixes only the RULE and the TIE-BREAK and then stops — it never defines the candidate set, never says whether a probability equal to the threshold predicts positive, and never gives beta or a formula for “F2”. That gap was reported as `PRE02` rather than filled quietly, and a human supervisor has now supplied **six** terms: candidate set = **exactly the distinct stored probabilities**; operator = **`probability >= threshold`**; objective = **F2, beta 2, `5*TP / (5*TP + 4*FN + FP)`** (the F-beta identity at beta 2, marked binding rather than advisory); **F2 = 0 when the denominator is 0**; **no rounding or truncation before selection**; output **round-trip exact** (`float(str(value)) == value`). Each is listed in the provenance record **with the reason it is untraceable**, and `supplied_terms_do_not_retroactively_create_a_frozen_rule = true` — the guarded failure mode is LAUNDERING, a 2026 decision acquiring the authority of a 2025 rule. Eight further terms ARE extracted and carry their source keys. **Input:** the pooled development-OOF surface pinned by SHA-256, filtered to `regularized_logistic_regression` / `logistic__C_0.1`, an expected **421** rows quoted from the primary development lock; no other input is authorized and no model is loaded or fitted — the derivation would read stored predictions only. **Selection:** highest F2 over the candidate set; among ties the **LARGEST** threshold, verified against the full argmax set rather than inferred from scan order. **`project/src/metrics.py::pick_threshold` is explicitly FORBIDDEN** and `TD09` enforces it: it is referenced by **zero** merged contracts, is **not imported** by the locked pipeline, builds a 200-point `linspace` grid unioned with the observed values and synthetic `0.0`/`1.0` endpoints from a `grid_points` **default argument** no contract fixes, and its ascending `if v > best_v` scan keeps the FIRST and therefore **LOWEST** maximizing candidate — directly contradicting the locked `higher_threshold` tie-break. Using it would have violated the very rule it appears to implement. **Eighteen fail-closed controls TD01–TD18, each `ABORT_THRESHOLD_DERIVATION`**, including candidate-set identity, the `>=` operator, the closed form with its zero-denominator convention, the no-rounding rule, the upward tie-break checked against the argmax set, round-trip-exact recording, `model_fits_executed == 0` and `final_test_rows_read == 0`. **This does NOT resolve `PRE02`:** `PRE02` requires the threshold **VALUE** in a committed artifact, and defining how a number would be produced is not producing it. `PRE01` and `PRE02` both remain **UNRESOLVED**, `final_test_contract_fully_executable` stays `false`, and the item 35 contract is byte-identical and unmodified. **Locking authorizes nothing:** `threshold_derivation_authorized = false`, `model_fit_authorized = false`, `final_test_access_authorized = false`, `final_test_rows_read = 0`, `stage130_started = false`, `merge_authorized = false`, `ready_for_review_authorized = false`, and every governance counter — including `development_oof_probability_values_read` and `candidate_thresholds_evaluated` — is `0`. The pointer is `human_authorization_required_for_threshold_derivation_execution` with `authorized = false`. Package: `project/stage129/threshold_derivation_algorithm_contract_lock/`; interpretation: `project/stage129/threshold_derivation_algorithm_contract_lock/README_STAGE129_THRESHOLD_DERIVATION_ALGORITHM_CONTRACT_LOCK.md`; regression tests: `project/tests/test_stage129_threshold_derivation_algorithm_contract_lock.py`.

37. `stage129-threshold-derivation-abort-and-parse-rule-lock` — **ABORTED EXECUTION RECORD AND PROSPECTIVE PARSE RULE LOCK; zero threshold derived, zero probability or target values re-read, zero parse executed against data, zero model fits, zero recalibration, zero Stage130, zero Final Test access.** The one-time derivation authorized against the merged item 36 contract **was started and then ABORTED**. It began from `fbcc48b6`, passed `TD01`, `TD15`, `TD09`, `TD02`, `TD03`, `TD16` and `TD12`, and failed building the candidate set: **every `predicted_probability` in the frozen OOF file is stored as a numpy repr string, not a number** — all **1263** rows, all three model families, while `observed_target`, `target_year` and the aggregation column are clean. The run was **not forced through**, because stripping the wrapper requires a parse step no merged contract defines and `TD07` forbids transforming values before selection; inventing it mid-run would have made the threshold an artefact of an unpinned implementation choice. **The defect is serialization only — no digits were lost:** every token matched an anchored wrapper with a well-formed finite decimal inside and every parsed inner value satisfied `float(str(v)) == v`. **Counters recorded honestly, including the ones that are not zero:** attempts started **1**, completed **0**, **F2 candidates evaluated 0**, **thresholds selected or materialized 0**, `final_test_rows_read = 0`; inside the aborted run 1263 rows were read into memory, 421 rows survived the model filter, 421 `observed_target` values were classified, exactly **1** `float()` attempt was made on a probability token and **0** inner values parsed. The evaluable row count was computed in memory but never emitted, so it is carried as **`UNKNOWN_NOT_ZERO`** — provably at least 1 — and **no number was guessed**. The diagnostic reads performed *after* the abort, which did parse **421** inner values, are **disclosed** rather than folded away, because a counter must never be falsely zero. **The parse rule, locked prospectively:** a token is interpreted only on an anchored `fullmatch`; **no `eval`, `exec`, `ast.literal_eval` or general code parser** (the token merely resembles a Python expression, and evaluating it would execute repository data as code); the digit class is **`[0-9]` rather than `\d`**, because `\d` matches Unicode decimal digits and `np.float64(۰.۵)` would otherwise pass — not hypothetical in this repository, and the tests prove the hazard; `NaN`, `Inf`, whitespace, nested or repeated wrappers, prefixes, suffixes, bare decimals, empty tokens, non-ASCII digits and hexadecimal or underscore-separated literals all **abort**, with **no row-skipping, default, sentinel or imputation**; conversion is **IEEE-754 binary64** under the locked runtime's `numpy.float64` semantics with no rounding after parse and before selection. It is classified explicitly as **serialization interpretation, not cleaning, rounding, imputation or correction**. **The frozen file does not move:** byte-identical, no cleaned copy, no rewritten replacement, no cascade — its SHA-256 is pinned in **23** committed files including three merged Stage129 contracts, which is precisely why the rule exists. **Twelve fail-closed controls PP01–PP12, each `ABORT_THRESHOLD_DERIVATION`.** The item 36 contract stays in force and byte-identical in every other respect: candidate set, `>=`, `beta=2` closed form, argmax tie-break, no rounding, round-trip precision, `pick_threshold` prohibition. **This does NOT resolve `PRE02`:** `PRE01` and `PRE02` both remain **UNRESOLVED**, `final_test_contract_fully_executable` stays `false`, `threshold_value` stays `null`. Package: `project/stage129/threshold_derivation_abort_and_parse_rule_lock/`; interpretation: `project/stage129/threshold_derivation_abort_and_parse_rule_lock/README_STAGE129_THRESHOLD_DERIVATION_ABORT_AND_PARSE_RULE_LOCK.md`; regression tests: `project/tests/test_stage129_threshold_derivation_abort_and_parse_rule_lock.py`.

38. `stage129-threshold-derivation-execution` — **THE ONE-TIME CONTRACTED THRESHOLD DERIVATION, EXECUTED. Development-OOF only. Zero fits, zero refits, zero `predict_proba`, zero tuning, zero sensitivity analysis, zero bootstrap, zero recalibration, zero SHAP, zero p-values, zero model re-selection, zero Stage130, zero Final Test access.** Executes exactly what the contracts merged in **PR #92** (algorithm) and **PR #93** (parse rule) permit. This is the **second** attempt; the first aborted on the serialization defect and its record is preserved **verbatim**. **Result: threshold `0.426878838687`**, `F2 = 0.5916030534351145`, `TP 31 / FP 91 / FN 4`, from **421** unique candidates over **421** evaluable rows (35 positive / 386 negative). The 35 pooled development-OOF positives match the figure quoted in the frozen calibration clause — an independent check on the row selection — and F2 recomputes from the recorded counts as `155/262`. The argmax set had a **single** member, so the tie-break did not bind, but it was **executed and proved** rather than assumed: the maximum was computed first, every attaining candidate collected into an explicit argmax set, `max()` taken over it, and a post-check re-asserted no member exceeds the selection. **Exactly one run:** no computational dry run and no determinism re-run — the executor refuses to start without `--write` so a reflexive dry run cannot happen — and artifact correctness was established from the **written bytes and their hashes** plus the F2 recomputation, never by repeating the derivation. **Row selection preceded parsing:** 1263 rows read, 421 survived the `regularized_logistic_regression` / `logistic__C_0.1` filter, all 421 evaluable, and **only those 421 tokens were parsed** — the other 842 rows' tokens were never converted to numbers. Parsing used the locked anchored regex with **no `eval`, `exec` or `ast.literal_eval`**; 421 read, 421 parsed, none skipped, defaulted or imputed. Candidates are exactly the distinct parsed values — no grid, synthetic endpoints, midpoints, thinning, rounding or truncation; positive prediction is `probability >= threshold`; missing targets are never negative; F2 uses the binding closed form on integer counts; `metrics.py::pick_threshold` was neither imported nor called. **All 31 controls TD01–TD18 and PP01–PP12 PASS.** **Counters kept separate:** this action started 1, succeeded 1, selected 1 threshold and parsed 421 tokens with every other counter `0`; cumulatively **2 attempts started, 1 prior abort retained, 1 successful, 1 threshold materialized**. The frozen OOF file is **byte-identical** before and after with no cleaned or normalised copy, and the three locked development results are byte-identical. **The result is a DEVELOPMENT OPERATING POINT ONLY** — explicitly not model superiority, not a test statistic, no inference. **`PRE02` is now RESOLVED**, but **`PRE01` remains UNRESOLVED**, so `final_test_contract_fully_executable = false`, `final_test_access_authorized = false`, `final_test_execution_authorized = false`, `final_test_rows_read = 0`, `final_test_locked = true` and `stage130_started = false` — **the Final Test is not declared executable**, and resolving one of two prerequisites opens nothing. Package: `project/stage129/threshold_derivation_execution/`; executor: `project/src/stage129_threshold_derivation.py`; interpretation: `project/stage129/threshold_derivation_execution/README_STAGE129_THRESHOLD_DERIVATION_EXECUTION.md`; regression tests: `project/tests/test_stage129_threshold_derivation_execution.py`.
